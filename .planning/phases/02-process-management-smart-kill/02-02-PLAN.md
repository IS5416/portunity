---
phase: 02-process-management-smart-kill
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - port-core/src/process.rs
  - port-core/src/process/info.rs
  - port-core/src/models/process.rs
  - port-core/src/filter.rs
  - port-core/src/scanner/tcp.rs
  - port-core/src/scanner/udp.rs
  - port-core/src/scanner.rs
  - port-tui/src/message.rs
  - port-tui/src/app.rs
  - port-tui/src/main.rs
  - port-tui/src/update.rs
  - port-tui/src/components.rs
  - port-tui/src/components/detail_panel.rs
  - port-tui/src/components/ports.rs
autonomous: true
requirements: [PROC-06, PROC-07]
must_haves:
  truths:
    - "User presses d on a selected port and a 12-row non-modal overlay shows: name+PID (bold title with protection badge), status, owning port+protocol, executable path, command line, start time (chrono '09:41:12 31-Jul-2026'), parent PID, digital signature, protection tier, reason (when protected), and a kill hint row (PROC-06, D-05/D-06)"
    - "All 9 detail fields populate on panel open; fetch is on-demand (D-08: path via QueryFullProcessImageNameW, command line via NtQueryInformationProcess class 60, start time via GetProcessTimes creation FILETIME, parent PID via Toolhelp32) with per-field failure rendering '—' dim; non-admin sessions show limited fields ('—') per success criterion 1"
    - "Digital signature check is on-demand async (D-07): WinVerifyTrustEx in spawn_blocking, 'Verifying…' then Signed/Unsigned/Unknown; result cached per-PID in memory; cache invalidated on next scan; access-denied/failure shows 'Unknown'"
    - "Selection change while the panel is open refreshes panel content (D-06); j/k/up/down/s/g/G//f pass through to the table"
    - "If the process exits while the panel is open, the name renders strikethrough (SGR 9) and the Status row shows 'Exited' (ProcessExited on scan)"
    - "Table rows show a ◆ marker: status.error for built-in whitelist membership, status.warning for user whitelist membership — populated at scan time by the core post-pass (is_system_critical / user_protected), render stays O(1)"
    - "Row dimming is whitelist-driven: protected rows dim only in non-admin sessions; admin sessions render full brightness with the marker (supersedes the Phase 1 PID<1000/known-name heuristic)"
    - "Start time reuses the ProcessSnapshot creation FILETIME (PROC-07, D-08) — the detail fetch captures the same identity the kill verifies against"
  artifacts:
    - path: "port-core/src/process/info.rs"
      provides: "Detail fetchers: query_full_path (QueryFullProcessImageNameW), query_command_line (NtQueryInformationProcess class 60, two-call), query_start_time (GetProcessTimes FILETIME -> SystemTime), query_parent_pid (Toolhelp32), verify_signature (WinVerifyTrustEx); pure helpers filetime_to_systemtime + UNICODE_STRING extraction"
      exports: ["fetch_details", "verify_signature", "filetime_to_systemtime"]
    - path: "port-core/src/models/process.rs"
      provides: "ProcessInfo gains user_protected: bool (Assumption A4 — required for the ◆-warning marker and confirmation gate)"
      exports: ["ProcessInfo"]
    - path: "port-core/src/scanner.rs"
      provides: "scan_all() post-pass: builtin_match -> is_system_critical, user-entry basename match -> QueryFullProcessImageNameW -> user_match -> user_protected, inside one spawn_blocking scope"
    - path: "port-tui/src/components/detail_panel.rs"
      provides: "DetailPanelComponent — 12-row Clear-over overlay per UI-SPEC internal layout with all states (no-selection, loading, verifying, exited, truncated values)"
      exports: ["DetailPanelComponent"]
  key_links:
    - from: "port-tui/src/main.rs"
      to: "port-core/src/process/info.rs"
      via: "d key -> ToggleDetailPanel -> spawn_blocking fetch_details(pid) -> DetailDataLoaded; signature via verify_signature -> SignatureVerified (D-07 cache)"
      pattern: "fetch_details|verify_signature"
    - from: "port-core/src/scanner.rs scan_all"
      to: "port-core/src/process/whitelist.rs"
      via: "post-pass calls builtin_match + user_match with settings loaded fresh (D-15)"
      pattern: "builtin_match|user_match"
    - from: "port-tui/src/update.rs ScanComplete"
      to: "port-tui/src/app.rs"
      via: "ProcessExited detection (detail_pid absent from new list) + signature_cache invalidation (D-07/D-08)"
      pattern: "ProcessExited|signature_cache"
  assumptions:
    - "[FLAGGED ASSUMPTION] PROC-06: All 9 detail fields populate on panel open; non-admin sessions show limited fields ('—') per success criterion 1 — implemented via Option-per-field fetchers that render '—' on failure/denied"
    - "[FLAGGED ASSUMPTION] Research A2: WinVerifyTrust costs 10-100ms/file — the D-07 on-demand + cache design is correct regardless; if slower, the 'Verifying…' state persists until the result arrives"
    - "[FLAGGED ASSUMPTION] Research A6: NtQueryInformationProcess class 60 stability on Win 10/11 — psutil/wazuh production usage since 2019; failure renders '—'"
  prohibitions:
    - statement: "MUST NOT persist or export command-line arguments from the detail panel — they may contain secrets; display-only on user request, nothing written to disk or clipboard"
---

## Phase Goal

**As a** developer or operator, **I want to** inspect the full details of any port's owning process and terminate it with smart-kill escalation and whitelist-gated protection, **so that** I can free ports safely without ever accidentally crashing the system.

<objective>
## Plan 02-02 — Process detail inspection: core fetchers + detail panel + protection markers

The inspection slice. Task 1 builds the port-core detail fetchers (path, command line, start time, parent PID, digital signature — all async + spawn_blocking, all per-field failure-tolerant) plus the scan-time protection marker post-pass (is_system_critical / user_protected). Task 2 wires the 12-row detail panel overlay (`d` key), the ◆ table markers, whitelist-driven dimming, and exited-process strikethrough.

Purpose: PROC-06 (all 9 detail fields) + the render-time half of PROC-03/04 markers. The scan post-pass keeps `filter.rs`'s `system_only` filter truthful (CONTEXT integration point) and the ◆ markers render-time O(1).
Output: `d` opens a live-refreshing detail panel; the table visually marks protected processes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-management-smart-kill/02-CONTEXT.md
@.planning/phases/02-process-management-smart-kill/02-RESEARCH.md
@.planning/phases/02-process-management-smart-kill/02-UI-SPEC.md
@.planning/phases/02-process-management-smart-kill/02-PATTERNS.md
@.planning/phases/02-process-management-smart-kill/02-01-SUMMARY.md (uses ProcessSnapshot/whitelist modules from plan 02-01)

# Source files to read before modifying:
#   port-core/src/process.rs        — add `pub mod info;` + re-exports (plan 02-01 declared handle/whitelist/kill only)
#   port-core/src/process/handle.rs — ProcessSnapshot + snapshot_for (plan 02-01) — info.rs reuses creation FILETIME (D-08)
#   port-core/src/models/process.rs — ProcessInfo (add user_protected — 5 constructor blast sites listed below)
#   port-core/src/scanner.rs        — scan_all() post-pass insertion point (lines 39-68)
#   port-core/src/filter.rs         — make_conn test constructor (line 136-145) needs user_protected: false
#   port-core/src/scanner/tcp.rs    — constructors at 282-291, 321-330; udp.rs at 146-155, 177-186
#   port-tui/src/main.rs, app.rs, message.rs, update.rs, components.rs, components/ports.rs, components/filter_panel.rs (overlay template)

<interfaces>
Existing contracts (do not re-explore):

From port-core/src/models/process.rs:
    pub struct ProcessInfo { pub pid: u32, pub name: String, pub executable_path: Option<String>,
        pub command_line: Option<String>, pub start_time: Option<std::time::SystemTime>,
        pub is_signed: Option<bool>, pub is_system_critical: bool, pub parent_pid: Option<u32> }
    // Task 1 adds: pub user_protected: bool

From port-core/src/models/connection.rs (row model — panel reads port/protocol from the selected Connection):
    pub struct Connection { pub local: SocketAddr-ish fields..., pub process: ProcessInfo, ... }

From port-core/src/process/handle.rs (plan 02-01):
    pub struct ProcessSnapshot { pub pid: u32, pub creation_time: Option<FILETIME>, pub executable_path: Option<String> }
    pub fn snapshot_for(pid: u32) -> crate::Result<ProcessSnapshot>

From port-core/src/process/whitelist.rs (plan 02-01):
    pub fn builtin_match(pid: u32, basename: &str) -> Option<&'static str>
    pub fn user_match(path: &str, entries: &[String]) -> bool

windows-rs 0.62.2 verified signatures (from RESEARCH Code Examples 3-6; ntdll not in the crate — manual link):
    QueryFullProcessImageNameW(h: HANDLE, flags: u32, buffer: &mut [u16], size: *mut u32) -> Result<()>
    GetProcessTimes(h: HANDLE, creation: *mut FILETIME, ...) -> Result<()>
    CreateToolhelp32Snapshot(flags: TH32CS_SNAPPROCESS, pid: u32) -> Result<HANDLE>
    Process32FirstW/Process32NextW(snapshot, entry: *mut PROCESSENTRY32W) -> Result<()>  // entry.dwSize MUST be set first
    PROCESSENTRY32W { dwSize, ..., th32ParentProcessID: u32, szExeFile: [u16; 260], ... }
    WinVerifyTrustEx(hwnd: HWND, action: *const GUID, data: *mut WINTRUST_DATA) -> i32  // 0 = signed
    TRUST_E_NOSIGNATURE: 0x800B0100; WTD_UI_NONE = 2; WTD_REVOKE_NONE = 0; WTD_CHOICE_FILE = 1
    WTD_CACHE_ONLY_URL_RETRIEVAL = 0x100; WTD_STATEACTION_VERIFY = 1; WTD_STATEACTION_CLOSE = 2
    FILETIME -> epoch: (u64(high)<<32 | low) / 10_000_000 saturating_sub 11_644_473_600 -> UNIX_EPOCH + Duration
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detail fetchers (info.rs) + user_protected model field + scan-time protection markers</name>
  <read_first>port-core/src/process.rs (add pub mod info), port-core/src/process/handle.rs (ProcessSnapshot — creation FILETIME reuse per D-08), port-core/src/models/process.rs, port-core/src/scanner.rs (scan_all post-pass), port-core/src/scanner/tcp.rs + udp.rs (constructor sites), port-core/src/filter.rs (make_conn), port-core/src/config/settings.rs (load_settings for the post-pass), 02-RESEARCH.md (Code Examples 3-6, Pattern 4, Assumptions A2/A4/A6/A8)</read_first>
  <files>port-core/src/process.rs, port-core/src/process/info.rs, port-core/src/models/process.rs, port-core/src/filter.rs, port-core/src/scanner/tcp.rs, port-core/src/scanner/udp.rs, port-core/src/scanner.rs</files>
  <behavior>
    - filetime_to_systemtime: FILETIME 116444736000000000 -> 1970-01-01T00:00:00Z (epoch); a 2026 timestamp round-trips via chrono display "09:41:12 31-Jul-2026"
    - UNICODE_STRING extraction: defensive bounds — copy min(Length/2, allocation) wide chars; Buffer pointer outside the allocation -> None; trailing NULs dropped
    - failure per field -> None (renders "—"), never an Err that blanks the whole panel
    - details() on current_exe (std::env::current_exe) populates path/start/parent (integration test, Windows-gated)
    - scan post-pass: builtin_match hit -> is_system_critical = true; user-entry basename match -> full-path query -> user_match -> user_protected = true; PID 0/4 handled by builtin_match
  </behavior>
  <action>Build the detail fetch layer and the scan-time marker pass. Every Win32 call inside tokio::task::spawn_blocking (Pitfall #9); per-field fetchers return Option (failure renders "—" per UI-SPEC detail states — never a whole-panel error); top-level async fns return crate::Result.

1. port-core/src/process.rs: add `pub mod info;` + `pub use info::{fetch_details, verify_signature};` to the module decls from plan 02-01.

2. port-core/src/process/info.rs (NEW — RESEARCH Code Examples 3-6):
   - Pure helpers (inline #[cfg(test)] unit tests per filter.rs precedent): `pub fn filetime_to_systemtime(ft: FILETIME) -> Option<std::time::SystemTime>` (u64 compose: (high<<32)|low; secs = value / 10_000_000 saturating_sub 11_644_473_600; UNIX_EPOCH + Duration::from_secs); `fn extract_unicode_string(buffer: &[u16], length_bytes: u32) -> Option<String>` (defensive: length_bytes/2 capped at buffer.len(), String::from_utf16_lossy on the slice, trim trailing NULs).
   - Command line (PROC-06): manual FFI via `#[link(name = "ntdll")] unsafe extern "system" { fn NtQueryInformationProcess(handle: *mut c_void, class: u32, info: *mut c_void, len: u32, ret_len: *mut u32) -> i32; }` with `const PROCESS_COMMAND_LINE_INFORMATION: u32 = 60` and `const STATUS_INFO_LENGTH_MISMATCH: i32 = 0xC000_0004`. Two-call pattern: first call (null buffer, 0 len) -> STATUS_INFO_LENGTH_MISMATCH with ret_len = needed bytes; allocate ret_len u16s; second call; the returned UNICODE_STRING's Buffer points INTO the caller's buffer (Win 8.1+) — extract via extract_unicode_string against the allocation (bounds-check BEFORE dereferencing per RESEARCH). Needs only PROCESS_QUERY_LIMITED_INFORMATION — works against elevated processes. Failure (access denied / invalid NTSTATUS) -> None.
   - `pub async fn fetch_details(pid: u32) -> crate::Result<ProcessInfo>`: single spawn_blocking scope: (1) open QLI|SYNCHRONIZE handle; (2) executable_path = QueryFullProcessImageNameW (size-in/out pattern, retry up to 64KiB); (3) start_time = GetProcessTimes creation FILETIME -> filetime_to_systemtime — this is ALSO the ProcessSnapshot creation identity (D-08, PROC-07 — the caller keeps the FILETIME for the kill snapshot); (4) parent_pid = Toolhelp32: CreateToolhelp32Snapshot(TH32CS_SNAPPROCESS, 0), set entry.dwSize = size_of::<PROCESSENTRY32W>() before Process32FirstW, loop Process32NextW until Err, match pid -> th32ParentProcessID, CloseHandle(snapshot) ALWAYS (handle leak gotcha from PITFALLS.md); (5) is_signed left None here — populated by verify_signature on demand (D-07); (6) is_system_critical/user_protected computed from whitelist::protection_status shape: builtin_match(pid, basename) -> is_system_critical, else user_match(path, settings.whitelist) -> user_protected (fresh load_settings() per D-15; <1ms). Return ProcessInfo (name from the caller's row data or from ProcessResolver — fill from resolver if the row name is missing).
   - `pub async fn verify_signature(path: &str) -> Option<bool>`: spawn_blocking; WinVerifyTrustEx(WINTRUST_ACTION_GENERIC_VERIFY_V2) with WINTRUST_DATA { cbStruct, dwUIChoice: WTD_UI_NONE(2), fdwRevocationChecks: WTD_REVOKE_NONE(0), dwUnionChoice: WTD_CHOICE_FILE(1), pFile: &WINTRUST_FILE_INFO { cbStruct, pcwszFilePath: PCWSTR(path), ... } } + dwProvFlags WTD_CACHE_ONLY_URL_RETRIEVAL (Assumption A8: offline cache-first — no network hang); 0 -> Some(true); TRUST_E_NOSIGNATURE (0x800B0100) -> Some(false); any other HRESULT (access denied, revocation, corrupt catalog) -> None ("Unknown" per UI-SPEC). Resource cleanup: second WinVerifyTrustEx call with dwStateAction = WTD_STATEACTION_CLOSE (Pitfall 5). Add a code comment documenting the D-07 scope: "Signed" = any valid signature chain, NOT trusted-publisher semantics (Open Question 3 resolution).
   - Inline tests: filetime_to_systemtime epoch + known-2026 value; extract_unicode_string bounds (short buffer, Buffer beyond allocation, embedded NULs); integration-style test on current_exe asserting path/start/parent populate (Windows-gated via cfg(target_os = "windows")).

3. port-core/src/models/process.rs: add `pub user_protected: bool` after is_system_critical (Assumption A4 — deviation from the CONTEXT "no model changes" statement is deliberate and documented: the marker field is required to render the ◆-warning at scan time without per-row OpenProcess at render).

4. Compile-fix the 5 explicit struct-literal constructor sites — add `user_protected: false` (keep the existing is_system_critical expressions unchanged — the scan post-pass refines them):
   - port-core/src/filter.rs make_conn (lines ~136-145)
   - port-core/src/scanner/tcp.rs (lines ~282-291 and ~321-330)
   - port-core/src/scanner/udp.rs (lines ~146-155 and ~177-186)

5. port-core/src/scanner.rs scan_all() post-pass (RESEARCH Pattern 4): after the name-application loop (lines 59-65), add ONE spawn_blocking scope (the post-pass runs in the async context — Pitfall #9) that: (1) load_settings() fresh (D-15); (2) build user_entry_basenames: HashSet<String> of lowercased file_name()s from settings.whitelist; (3) for each connection: basename from conn.process.name; builtin_match(pid, basename) -> Some -> conn.process.is_system_critical = true; else if user_entry_basenames contains lowercased basename -> QueryFullProcessImageNameW on a QLI handle (open/close inside this scope) -> user_match(path, &settings.whitelist) -> conn.process.user_protected = true. Only same-basename processes pay the ~1ms path query. This keeps filter.rs system_only truthful and feeds the ◆ markers.
</action>
  <verify>
    <automated>cargo test -p port-core process::info && cargo test -p port-core filter && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p port-core process::info` green: filetime_to_systemtime epoch + 2026 values, UNICODE_STRING bounds cases, current_exe details integration test
    - `cargo test -p port-core filter` still green (make_conn updated, SRCH-02 filter behavior untouched)
    - `cargo build --workspace` compiles — any missed struct-literal constructor site surfaces as an exhaustive-literal compile error, so a green build proves all 5 sites carry user_protected
    - fetch_details returns all 9 ProcessInfo fields populated on current_exe; every per-field failure path returns None (not Err)
    - scanner.rs post-pass: a connection whose process basename is in BUILTIN gets is_system_critical = true; a connection matching a user whitelist path gets user_protected = true
  </acceptance_criteria>
  <done>Core detail fetching works for all 9 fields with per-field failure tolerance; scan output carries truthful protection markers; workspace compiles and tests green.</done>
</task>

<task type="auto">
  <name>Task 2: d key opens the 12-row detail panel; table gains ◆ markers, whitelist dimming, exited strikethrough</name>
  <read_first>port-tui/src/main.rs (map_key_event 219-317, ElevateRequest intercept 156-179, render_app overlay placement 395-431, status bar 483-580, footer 583-676), port-tui/src/app.rs, port-tui/src/message.rs, port-tui/src/update.rs, port-tui/src/components.rs, port-tui/src/components/filter_panel.rs (label-row + Clear-over pattern), port-tui/src/components/ports.rs (process cell 196-266, SYSTEM_NAMES 361-382, truncate 385-392, render_scrollbar 395-435), port-tui/src/theme.rs, 02-UI-SPEC.md (Detail Panel Internal Layout 12 rows, Typography strikethrough delta, Color protection semantics, Detail Panel States copy, pass-through table, Message Additions, footer strings)</read_first>
  <files>port-tui/src/message.rs, port-tui/src/app.rs, port-tui/src/main.rs, port-tui/src/update.rs, port-tui/src/components.rs, port-tui/src/components/detail_panel.rs, port-tui/src/components/ports.rs</files>
  <action>Wire the detail panel overlay and the table protection markers. The d key and panel refresh flow replicate the Phase 1 overlay + ElevateRequest patterns.

1. port-tui/src/message.rs: add `ToggleDetailPanel`, `DetailDataLoaded { process_info: ProcessInfo, creation_time: Option<FILETIME> }` (creation_time rides along so the App can build the kill snapshot from the D-08 fetch — PROC-07 identity reuse), `SignatureVerified { pid: u32, is_signed: Option<bool> }`, `ProcessExited { pid: u32 }`. Import port_core::models::ProcessInfo and windows::Win32::Foundation::FILETIME (windows crate is a port-tui dep via workspace — verify; if not directly usable, carry the raw u64 composed value instead: `creation_ft: Option<u64>`).

2. port-tui/src/app.rs: add fields (all in App::new()): `pub detail_active: bool`, `pub detail_pid: Option<u32>` (row the panel shows; selection change refreshes — D-06), `pub detail_data: Option<ProcessInfo>`, `pub detail_loading: bool`, `pub detail_snapshot_creation: Option<u64>` (from DetailDataLoaded; the kill snapshot base), `pub signature_cache: HashMap<u32, Option<bool>>` (D-07: pid -> verified result; populated on SignatureVerified, cleared on ScanComplete). Add accessor `pub fn selected_connection(&self) -> Option<&Connection>` (mirrors display_data(); the panel reads port/protocol/name from the selected row).

3. port-tui/src/main.rs map_key_event: (a) DEFAULT-mode: `KeyCode::Char('d')` -> if app.active_tab == 1 (Ports tab, UI-SPEC) -> Some(Message::ToggleDetailPanel) else None; (b) DETAIL overlay dispatch inserted after the filter block, before the default match: `if app.detail_active { match key.code { 'd'|Esc -> ToggleDetailPanel, _ => {} } }` — everything else (j/k/up/down/r/s/g/G//f and x) passes through to the default match per UI-SPEC detail pass-through table.

4. port-tui/src/main.rs event loop (ElevateRequest template): (a) inbound intercept on ToggleDetailPanel when OPENING (guard app.detail_loading): spawn_blocking { fetch_details(pid) } -> on Ok send DetailDataLoaded { process_info, creation_time: raw_creation_ft }; on Err send ScanError-adjacent handling: keep the panel open showing name/PID from the row with fields at "—" (UI-SPEC error state: close and re-open to retry); (b) inbound intercept on MoveUp/MoveDown/ScrollTop/ScrollBottom when app.detail_active and the selected row's pid changed: after update(app, m), trigger the same fetch for the new selection (D-06 live refresh); (c) inbound intercept on DetailDataLoaded is NOT needed (drain path — update() stores it); the signature step: in update()'s DetailDataLoaded handler, if process_info.executable_path is Some(path) and signature_cache misses pid -> the handler can't spawn — so the drain loop special-case (mirroring ScanComplete at lines 184-190): when DetailDataLoaded arrives and the signature cache misses, spawn_blocking { verify_signature(path) } -> send SignatureVerified { pid, is_signed }; if the cache hits, render immediately. (d) drain special-case on ScanComplete already exists — extend it: clear app.signature_cache (D-07), and if app.detail_pid is Some(pid) and pid is NOT in the new connection list -> send ProcessExited { pid } to update().

5. port-tui/src/update.rs handlers: ToggleDetailPanel -> toggle detail_active; opening sets detail_pid = selected row pid, detail_loading = true (fetch fires via the intercept); closing clears detail_loading. DetailDataLoaded -> store detail_data + detail_snapshot_creation + detail_loading = false. SignatureVerified -> signature_cache.insert(pid, is_signed) + if detail_pid == Some(pid) update detail_data.is_signed. ProcessExited { pid } -> if detail_pid == Some(pid): mark the panel exited state (set a flag consumed by the component: reuse detail_data with a side flag `detail_exited: bool` on App — the component renders name strikethrough + Status "Exited"). ScanComplete handler: the signature_cache clear + ProcessExited emission happen in the drain special-case (step 4d) — update() itself only applies state.

6. port-tui/src/components/detail_panel.rs (NEW — filter_panel.rs Clear-over pattern): stateless DetailPanelComponent rendering the UI-SPEC 12-row internal layout:
   Row 0 title: "{name} — PID {pid}" (Bold, fg_emphasis) + protection badge: built-in -> "[PROTECTED]" Bold status.error on bg_overlay; user -> "[CONFIRM]" Bold status.warning (from detail_data.is_system_critical / user_protected); trailing "[Esc]" muted. Name truncates with U+2026 to fit the 80-col minimum.
   Row 1 Status: "Running" | "Exited" (strikethrough state when detail_exited).
   Row 2 Owning port: "{local_port} ({protocol})" from the selected Connection (app.selected_connection()).
   Rows 3-9 (label column fixed 17 chars, "Label:" + space): Executable path (right-segment-preserving truncation "…\dir\name.exe"), Command line (right-truncate U+2026), Start time (chrono "09:41:12 31-Jul-2026" — format "%H:%M:%S %d-%b-%Y"), Parent PID, Signature ("Verifying…" fg_muted when detail_loading or cache-miss; "Signed" status.success; "Unsigned" status.warning; "Unknown" fg_muted), Protection ("Not protected" | "System-critical (built-in whitelist)" | "On your protection list"), Reason (only when protected: built-in -> "{name} is a core Windows system process. Terminating it would crash or destabilize the system."; user -> "{name} is on your protection list."). Unavailable fields render "—" Dim fg_muted.
   Row 10 hint (accent_secondary per accent-split rule): "Press x to terminate" | "Press x to terminate — confirmation required" | "Cannot terminate — system-critical".
   Row 11: bottom border line (U+2500 run).
   States: no selection -> "No port selected — use j/k to move through the list" muted body; loading -> "Loading details…" in place of field values (name/PID from scan data show immediately per UI-SPEC).
   All copy from the UI-SPEC Copywriting Contract tables verbatim.

7. port-tui/src/components/ports.rs (three edit zones): (1) process cell (lines 196-266): prepend "◆ " (U+25C6) when conn.process.is_system_critical (fg = status.error) or conn.process.user_protected (fg = status.warning), then the name with normal fg; (2) supersede the Phase 1 heuristic: DELETE the SYSTEM_NAMES const + is_system_process() (lines 361-382); compute `let protected = conn.process.is_system_critical || conn.process.user_protected; let system_dim = !app.is_admin && protected;` — dim protected rows only when non-admin (UI-SPEC Protection Semantics; admin renders full brightness with the marker); (3) strikethrough: when conn.process.pid == app.last_killed_pid (set by plan 02-01 Task 2 on successful kill) add Modifier::CROSSED_OUT to the process cell (SGR 9 — UI-SPEC Typography delta; row pending scan removal). truncate() (385-392) stays.

8. port-tui/src/components.rs: add `pub mod detail_panel;` + `pub use detail_panel::DetailPanelComponent;`.

9. port-tui/src/main.rs render_app overlay placement (lines 395-431): after the existing search/filter renders, add `if app.detail_active { let detail_area = Rect { height: 12, ..content_area }; DetailPanelComponent.render(app, f, detail_area, theme); }` — 12-row top-anchored Clear-over (D-05: table is never squeezed; ≥9 table rows stay visible at 80x24). Stack order per UI-SPEC: table -> search -> filter -> detail -> (whitelist, plan 02-03) -> (confirm, plan 02-01).

10. port-tui/src/main.rs footer: add the detail-panel branch (before the default footer): "[Esc]Close [j/k]Next port [x]Kill [r]Refresh  —  detail for {name}" with {name} truncated U+2026 to (term_width - 66) chars (UI-SPEC footer table).
</action>
  <verify>
    <automated>cargo test --workspace && cargo build -p port-tui</automated>
    <human-check>Run `cargo run -p port-tui`: select a row, press d — 12-row panel shows all fields (path, cmdline, start time, parent PID, signature after "Verifying…", protection, reason when protected); j/k while open refreshes the panel; kill the process then wait for scan — the row shows strikethrough, the panel shows "Exited"; protected rows show the ◆ marker (color per tier) and dim in a non-admin session.</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` green; `cargo build -p port-tui` compiles
    - map_key_event: 'd' bound (Ports tab), detail overlay dispatch intercepts d/Esc only, pass-through intact (x/j/k//f/r/s/g/G)
    - Panel flow: ToggleDetailPanel -> fetch_details spawn_blocking -> DetailDataLoaded -> render; signature verified on-demand with per-PID cache cleared on ScanComplete; ProcessExited fires when detail_pid leaves the scan list
    - ports.rs: no known-name heuristic constant or its helper fn remains (grep -c 'SYSTEM_NAMES' port-tui/src/components/ports.rs == 0); ◆ marker + non-admin dimming + strikethrough driven from model fields / last_killed_pid

<!-- planner-discipline-allow: SYSTEM_NAMES -->
    - Detail overlay renders top-anchored 12 rows; footer detail string uses the term_width - 66 budget
  </acceptance_criteria>
  <done>d opens a live-refreshing 12-row detail panel with all 9 fields, signature verification, protection badge and exited detection; the table marks protected processes and strikes through killed rows; tests green.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 02-02)

New symbols, files, and config this plan creates:

| Kind | Symbol / File | Notes |
|------|---------------|-------|
| fn | `info::fetch_details(pid) -> Result<ProcessInfo>` | Async, one spawn_blocking scope (D-08) |
| fn | `info::verify_signature(path) -> Option<bool>` | WinVerifyTrustEx, WTD_STATEACTION_CLOSE cleanup, cache-first (A8) |
| fn | `info::filetime_to_systemtime(FILETIME) -> Option<SystemTime>` | Pure, unit-tested |
| fn | `info::extract_unicode_string(&[u16], u32) -> Option<String>` | Defensive bounds (A6) |
| extern | `NtQueryInformationProcess` (ntdll manual link, class 60) | Two-call pattern, no PROCESS_VM_READ |
| const | `PROCESS_COMMAND_LINE_INFORMATION: u32 = 60`, `STATUS_INFO_LENGTH_MISMATCH` | |
| field | `ProcessInfo.user_protected: bool` | A4 — 5 constructor sites updated |
| post-pass | `scanner::scan_all()` protection markers | spawn_blocking; builtin/user tier matching |
| messages | `ToggleDetailPanel, DetailDataLoaded, SignatureVerified, ProcessExited` | |
| app fields | `detail_active, detail_pid, detail_data, detail_loading, detail_snapshot_creation, detail_exited, signature_cache` | |
| fn | `app::selected_connection() -> Option<&Connection>` | |
| component | `DetailPanelComponent` | 12-row Clear-over per UI-SPEC |
| render | ports.rs ◆ marker + whitelist dimming + strikethrough | SYSTEM_NAMES deleted |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core detail fetchers (info.rs) -> Win32 (ntdll/toolhelp/wintrust) | Untrusted process data (command lines, paths) crosses into the panel; display-only |
| scanner post-pass -> whitelist.rs + settings.toml | Scan output protection markers derive from user-writable settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | info.rs detail panel | medium | accept | Command lines may contain secrets — display-only, user-initiated local view, no persistence, no export, no clipboard (prohibition P4); Help overlay copy (plan 02-03) warns users; redaction explicitly out of scope per RESEARCH Security Domain |
| T-02-07 | Spoofing | info.rs verify_signature | medium | mitigate | WinVerifyTrust result mapped 3-way (Signed/Unsigned/Unknown); "Verifying…" state until result; per-PID cache invalidated on every scan (D-07) — no stale signature displayed |
| T-02-09 | Tampering | info.rs command-line extraction | medium | mitigate | Defensive UNICODE_STRING extraction: bounds-check Buffer against the allocation before dereferencing (RESEARCH Code Example 3) — a corrupt/malicious NtQueryInformationProcess return cannot read out of bounds |
| T-02-01 | Spoofing / Integrity | info.rs + kill.rs (cross-plan) | high | mitigate | Detail fetch captures the same creation FILETIME the kill verifies against (D-08, PROC-07) — the panel identity and the kill identity are the same snapshot; full mitigation lives in plan 02-01 kill.rs |
| T-02-05 | Tampering | scanner.rs post-pass (cross-plan) | high | mitigate | Marker truthfulness: builtin_match checked before user tier (Pitfall 6); is_system_critical/user_protected derive from the same whitelist.rs logic the kill gate uses (plan 02-01) |
| T-02-SC | Tampering | cargo build | high | mitigate | Zero new crates (windows-rs 0.62.2 features already added in plan 02-01; ntdll linked via #[link] from the OS, not a crate) |
</threat_model>

<verification>
- `cargo test -p port-core process::info` — FILETIME/UNICODE_STRING/conversions (VALIDATION map TBD-08)
- `cargo test -p port-core filter` — SRCH-02 filter behavior untouched (TBD-09)
- `cargo test --workspace` — full suite green before wave merge
- Manual UAT deferred to end-of-phase: overlay render states, key mapping, protection markers (VALIDATION map TBD-10)
</verification>

<success_criteria>
- User presses d and sees all 9 detail fields for any selected port's process, with truthful signature/protection status and graceful degradation for non-admin sessions
- The table visually marks protected processes (◆ per tier) and dims them for non-admin; killed processes show strikethrough until the scan removes them
- The detail identity (creation FILETIME) is the same snapshot the kill verifies against — no panel/kill identity split
</success_criteria>

<output>
Create `.planning/phases/02-process-management-smart-kill/02-02-SUMMARY.md` when done
</output>
