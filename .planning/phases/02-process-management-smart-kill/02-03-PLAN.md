---
phase: 02-process-management-smart-kill
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - port-core/src/process/whitelist.rs
  - port-tui/src/message.rs
  - port-tui/src/app.rs
  - port-tui/src/main.rs
  - port-tui/src/update.rs
  - port-tui/src/components.rs
  - port-tui/src/components/whitelist_overlay.rs
  - port-tui/src/components/help.rs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PROC-05, SRCH-02]
must_haves:
  truths:
    - "User presses w and a 20-row non-modal overlay shows a read-only built-in section (◆ basename + short plain-language reason per entry) and an editable user section (full executable paths); add via the 'Path: >_' input row, delete selected with d (D-14, UI-SPEC whitelist layout)"
    - "Whitelist changes take effect immediately without restart: settings.toml is re-read before every kill attempt (D-15), and the w overlay calls save_settings on add/remove"
    - "Adding an invalid or nonexistent path shows a status-bar error and the entry is not added (UI-SPEC backstop: absolute-path syntax required, quotes stripped, 8.3 resolved via GetLongPathNameW when the file exists, control chars rejected, length capped)"
    - "Adding a duplicate path is a no-op; removing a non-existent entry is a no-op; settings.toml round-trips without duplicates (PROC-05)"
    - "User whitelist entries are confirmation-level only — no hard-block option (D-12); removal is instant with no confirmation (reversible by re-adding, UI-SPEC Destructive Actions)"
    - "Whitelist mutations occur only on the main TUI event loop; kill-time re-read sees the last written state (PROC-05 concurrency)"
    - "Built-in entries cannot be added, edited, or removed from the overlay — read-only section (D-13); attempting to kill a built-in process never opens the confirm dialog (D-09)"
    - "The '?' key opens a Help overlay documenting all keys including [d] Detail, [x] Kill, [w] Whitelist, [s] Sort and the L2-confirm [y]/[n] — the canonical reference for footer-dropped keys (UI-SPEC)"
    - "SRCH-02 (AND-across/OR-within filter combination) is already implemented in port-core/src/filter.rs from Phase 1 — this phase verifies the existing test suite and updates the traceability records only; no filter engine changes (compile-fix constructor update from plan 02-02 is the only filter.rs delta)"
  artifacts:
    - path: "port-core/src/process/whitelist.rs"
      provides: "validate_user_entry (absolute-path check, control-char rejection, quote trim, 8.3 via GetLongPathNameW, existence check, length cap) + normalize_user_entry"
      exports: ["validate_user_entry", "normalize_user_entry"]
    - path: "port-tui/src/components/whitelist_overlay.rs"
      provides: "WhitelistOverlayComponent — 20-row Clear-over: built-in list (9 rows read-only), user list (selectable, scrollbar), Path input row, hint row"
      exports: ["WhitelistOverlayComponent"]
    - path: "port-tui/src/components/help.rs"
      provides: "HelpComponent — full key reference overlay ('?' key), including Phase 2 additions and the command-line-secrets note"
      exports: ["HelpComponent"]
  key_links:
    - from: "port-tui/src/main.rs"
      to: "port-core/src/config/settings.rs"
      via: "w overlay add/delete -> spawn_blocking validate + save_settings -> WhitelistSaved -> next kill's D-15 re-read sees it"
      pattern: "save_settings|validate_user_entry"
    - from: "port-tui/src/update.rs"
      to: "port-tui/src/app.rs"
      via: "WhitelistSaved applies the mutation to app.whitelist_settings + status bar Added/Removed strings (status.info)"
      pattern: "WhitelistSaved|whitelist_settings"
    - from: "port-tui/src/main.rs map_key_event"
      to: "port-tui/src/components/help.rs"
      via: "'?' -> Message::ToggleHelp -> HelpComponent render"
      pattern: "ToggleHelp"
  assumptions:
    - "[FLAGGED ASSUMPTION] PROC-05 input-validation strictness (RESEARCH Open Question 2): adding requires absolute-path syntax, strips quotes, resolves 8.3 via GetLongPathNameW when the file exists, and rejects nonexistent paths per the approved UI-SPEC backstop — a path may be valid later (app being updated) but the backstop says error, not added"
    - "[FLAGGED ASSUMPTION] Research A9: whitelist-overlay copy and status-bar add/remove strings must fit 80 cols; the duplicate-add no-op uses the info-tone string '{path} is already on your protection list' (implementation copy consistent with the UI-SPEC style — UAT-confirmed at end of phase)"
  prohibitions:
    - statement: "MUST NOT accept a user whitelist entry without normalization (quotes, trailing separators, case, 8.3) — protection matching must never be bypassed by path trickery"
---

## Phase Goal

**As a** developer or operator, **I want to** inspect the full details of any port's owning process and terminate it with smart-kill escalation and whitelist-gated protection, **so that** I can free ports safely without ever accidentally crashing the system.

<objective>
## Plan 02-03 — Whitelist management overlay + Help overlay + SRCH-02 traceability

The management slice. Task 1 builds the `w` overlay: read-only built-in list (what is protected and why), editable user list with validated add-by-path and instant-effect save (D-13/D-14/D-15). Task 2 creates the Help overlay (`?` key — the canonical reference for the footer-dropped `s`/`w` keys per UI-SPEC) and closes SRCH-02 (verification + traceability only — the filter engine was implemented in Phase 1 and must NOT be re-implemented).

Purpose: PROC-05 (user-customizable whitelist with immediate effect) + the phase's documentation closure (REQUIREMENTS.md/ROADMAP.md traceability).
Output: users manage their protection list from the TUI with no restart; every keyboard capability is documented; SRCH-02 is marked Complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-management-smart-kill/02-CONTEXT.md
@.planning/phases/02-process-management-smart-kill/02-RESEARCH.md
@.planning/phases/02-process-management-smart-kill/02-UI-SPEC.md
@.planning/phases/02-process-management-smart-kill/02-PATTERNS.md
@.planning/phases/02-process-management-smart-kill/02-01-SUMMARY.md (whitelist.rs Protection/BUILTIN from plan 02-01)
@.planning/phases/02-process-management-smart-kill/02-02-SUMMARY.md (message/app/main/update patterns from plans 02-01/02-02)

# Source files to read before modifying:
#   port-core/src/process/whitelist.rs — plan 02-01 output; gains validate_user_entry + normalize_user_entry
#   port-core/src/config/settings.rs   — save_settings() (the overlay's persistence call, D-15)
#   port-tui/src/main.rs, app.rs, message.rs, update.rs, components.rs — kill + detail wiring from plans 02-01/02-02 (extend, do not restructure)
#   port-tui/src/components/search.rs  — block-cursor input row pattern (lines 55-73)
#   port-tui/src/components/ports.rs   — render_scrollbar helper (lines 395-435) for the two lists
#   port-core/src/filter.rs            — existing SRCH-02 implementation + tests (verify only — NO behavior change)

<interfaces>
Existing contracts (do not re-explore):

From port-core/src/process/whitelist.rs (plan 02-01):
    pub struct BuiltinEntry { pub name: &'static str, pub reason: &'static str }
    pub const BUILTIN: &[BuiltinEntry]                      // >=25 entries
    pub fn builtin_match(pid: u32, basename: &str) -> Option<&'static str>
    pub fn user_match(path: &str, entries: &[String]) -> bool
    pub enum Protection { None, UserConfirm, HardBlocked(&'static str) }

From port-core/src/config/settings.rs:
    pub struct AppSettings { pub admin_detected: bool, pub schema_version: i32,
                             pub whitelist: Vec<String>, pub kill_timeout_secs: u64 }
    pub fn load_settings() -> crate::Result<AppSettings>; pub fn save_settings(&AppSettings) -> crate::Result<()>

From port-tui/src/message.rs (plans 02-01/02-02 — extend, don't restructure):
    existing Phase 2 variants: Kill, KillPrepared, KillConfirmed, KillCancelled, KillStart, KillTimeout,
    KillExecute, KillOutcome, ToggleDetailPanel, DetailDataLoaded, SignatureVerified, ProcessExited

From port-tui/src/app.rs (plans 02-01/02-02):
    kill_status: Option<KillStatus>, kill_in_flight: bool, confirm_pid/name/port, pending_kill_snapshot,
    last_killed_pid, detail_active, detail_pid, detail_data, detail_loading, detail_exited,
    signature_cache: HashMap<u32, Option<bool>>, selected_connection(), display_data()

Windows-rs 0.62.2 (verified): GetLongPathNameW(path: PCWSTR, buffer: &mut [u16], size: u32) -> u32
(returns 0 + ERROR_INVALID_PARAMETER/ERROR_FILE_NOT_FOUND when the path does not exist — the 8.3 resolver only applies to existing files)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: w overlay — read-only built-in list, validated user add/remove, instant effect</name>
  <reversibility rating="costly">Implements D-13 (whitelist persisted in settings.toml — moving user entries to a different store later requires a settings.toml migration plus GUI adaptation in Phases 5/6) and D-14 (w-key overlay surface — Phase 6 settings center reuses the same data structure). User-selected in CONTEXT; ratings carried for the record — no checkpoint.</reversibility>
  <read_first>port-tui/src/components/search.rs (block-cursor input pattern 55-73), port-tui/src/components/filter_panel.rs (Clear-over + focus + field-row pattern), port-tui/src/components/ports.rs (render_scrollbar 395-435), port-tui/src/main.rs (map_key_event 219-317, intercept 156-179, render_app overlays 395-431, footer 583-676), port-tui/src/app.rs, port-tui/src/message.rs, port-tui/src/update.rs, port-tui/src/components.rs, port-core/src/process/whitelist.rs, port-core/src/config/settings.rs, 02-UI-SPEC.md (Whitelist Overlay layout/copy, pass-through table, Focus Management, Message Additions, footer strings), 02-RESEARCH.md (Open Question 2 validation recommendation)</read_first>
  <files>port-core/src/process/whitelist.rs, port-tui/src/message.rs, port-tui/src/app.rs, port-tui/src/main.rs, port-tui/src/update.rs, port-tui/src/components.rs, port-tui/src/components/whitelist_overlay.rs</files>
  <behavior>
    - validate_user_entry("C:\\Program Files\\Foo\\bar.exe") -> Ok (existing file, 8.3 resolved)
    - validate_user_entry("  \"C:\\Program Files\\Foo\\bar.exe\"  ") -> Ok (quotes/whitespace stripped)
    - validate_user_entry("not-a-path") / ("C:\\missing\\thing.exe" nonexistent) / (contains control char) -> Err(reason)
    - length cap: > 4096 chars -> Err
    - duplicate add (already in whitelist) -> no-op, no duplicate in settings.toml round-trip
    - built-in names cannot appear in the user list workflow (built-in tier wins at match time — Pitfall 6)
  </behavior>
  <action>Build the whitelist management surface. The validation logic lands in port-core (Win32 call GetLongPathNameW must stay in port-core per CORE-01/Anti-Pattern #2); the overlay replicates the Phase 1 search/filter overlay patterns.

1. port-core/src/process/whitelist.rs (extend plan 02-01 module; D-10/D-15, RESEARCH Open Question 2 resolution):
   - `pub fn normalize_user_entry(input: &str) -> Option<String>`: trim whitespace; strip ONE pair of surrounding double quotes if present; reject if empty, if it contains any control char (ch.is_control()), or if it is not absolute (no drive prefix like "C:" or UNC "\\\\" start); when the file exists, resolve 8.3 via GetLongPathNameW (Win32 — caller must be inside spawn_blocking; document this); strip one trailing '\' or '/'; return the normalized form. Case is preserved as-typed (comparison is case-insensitive in user_match).
   - `pub fn validate_user_entry(input: &str) -> std::result::Result<String, String>`: normalize_user_entry -> Err with a human-readable reason on each rejection ("Path must be absolute (C:\\... or \\\\server\\share\\...)", "Path contains control characters", "Path does not exist", "Path is too long (max 4096)") — existence check via std::fs::metadata on the normalized path; return Err("Path does not exist") for nonexistent paths per the approved UI-SPEC backstop. Both fns are pure except the GetLongPathNameW call — keep the 8.3 resolution inside a `#[cfg(target_os = "windows")]` helper so the pure parts unit-test on any host.
   - Inline #[cfg(test)]: normalization matrix (quotes, whitespace, trailing separator, control chars, relative path rejection), validation reasons, duplicate semantics (adding an entry equal (case-insensitive) to an existing one returns the existing entry — the caller treats it as a no-op).

2. port-tui/src/message.rs: add `ToggleWhitelistOverlay`, `WhitelistFocusNext`, `WhitelistFocusPrev`, `WhitelistSelectMove { dir: i8 }`, `WhitelistDeleteSelected`, `WhitelistInput(char)`, `WhitelistBackspace`, `WhitelistCursorMove { dir: i8 }`, `WhitelistAdd { path: String }`, `WhitelistSaved { path: String, added: bool }`, `WhitelistError { path: String, reason: String }`, `ToggleHelp` (per UI-SPEC Message Additions list).

3. port-tui/src/app.rs: add `pub enum WhitelistFocus { List, Input }` with next()/prev() methods (FilterField cycle pattern from message.rs lines 50-84) and fields (all initialized in App::new()): `whitelist_active: bool`, `whitelist_focus: WhitelistFocus`, `whitelist_selected: usize`, `whitelist_input: String`, `whitelist_input_cursor: usize`, `whitelist_settings: AppSettings` (working copy), `help_active: bool`.

4. port-tui/src/main.rs map_key_event: (a) DEFAULT-mode: `KeyCode::Char('w')` -> Some(Message::ToggleWhitelistOverlay) (any tab, UI-SPEC); `KeyCode::Char('?')` -> Some(Message::ToggleHelp); (b) WHITELIST overlay dispatch inserted after the detail block, before the default match (UI-SPEC whitelist pass-through table): `if app.whitelist_active { match key.code { Esc -> ToggleWhitelistOverlay, Tab -> WhitelistFocusNext, BackTab -> WhitelistFocusPrev, 'j'|Down when focus == List -> WhitelistSelectMove{dir:1}, 'k'|Up when focus == List -> WhitelistSelectMove{dir:-1}, 'd' when focus == List -> WhitelistDeleteSelected, 'Enter' when focus == Input -> WhitelistAdd{path: app.whitelist_input.clone()}, 'Backspace' when focus == Input -> WhitelistBackspace, Left when focus == Input -> WhitelistCursorMove{dir:-1}, Right when focus == Input -> WhitelistCursorMove{dir:1}, Char(ch) when focus == Input && !ch.is_control() -> WhitelistInput(ch), _ -> {} } }` — j/k/up/down do NOT pass through when the user list is focused (declared UI-SPEC exception: the overlay covers the table); r/s and tab/quit keys still pass (r/s land in the default match below — keep the default match reachable by falling through for those).

5. port-tui/src/main.rs event loop (ElevateRequest template): (a) inbound intercept on ToggleWhitelistOverlay: no special handling (the open load is synchronous per the UI-SPEC backstop — settings.toml read is <1ms and the spec explicitly declares "no loading state exists by design"; document this as the one allowed sync file read on the runtime); (b) inbound intercept on WhitelistAdd { path }: guard !app.kill_in_flight; spawn_blocking { let result = validate_user_entry(&path); match result { Ok(normalized) -> { let mut s = load_settings()... hmm — the working copy lives in app.whitelist_settings; the save must persist the WORKING copy + the new entry. Use the following contract: clone app.whitelist_settings into the closure; if whitelist (case-insensitive) already contains normalized -> send WhitelistSaved { path: normalized, added: false } (no-op, no save); else push normalized + save_settings(&mut) -> Ok -> send WhitelistSaved { path: normalized, added: true }; Err(e) -> send WhitelistError { path, reason: e } }; Err from save_settings -> send WhitelistError { path, reason: "could not save settings: {e}" } } (the save runs inside spawn_blocking — file I/O + Win32 off the runtime); (c) inbound intercept on WhitelistDeleteSelected: if app.whitelist_selected < app.whitelist_settings.whitelist.len(): clone working copy, remove the entry at whitelist_selected, save_settings -> WhitelistSaved { path: removed_path, added: false } / WhitelistError (removal of a non-existent index is a no-op — bounds-check before spawning); (d) drain special-case: none needed (WhitelistSaved/WhitelistError are plain update() state).

6. port-tui/src/update.rs handlers: ToggleWhitelistOverlay -> toggle whitelist_active; on open: whitelist_settings = load_settings() (sync — UI-SPEC backstop, see step 5a), whitelist_focus = List, whitelist_selected = 0, whitelist_input.clear(), whitelist_input_cursor = 0; on close: nothing else. WhitelistFocusNext/Prev -> whitelist_focus = whitelist_focus.next()/prev(). WhitelistSelectMove { dir } -> move whitelist_selected within bounds of the user list (0..len, clamp). WhitelistDeleteSelected -> no-op here (intercept-owned; the entry is removed when WhitelistSaved arrives). WhitelistInput(ch) -> insert at whitelist_input_cursor (String::insert, cursor += 1). WhitelistBackspace -> remove char before cursor. WhitelistCursorMove { dir } -> move cursor within 0..=len. WhitelistAdd -> no-op (intercept-owned). WhitelistSaved { path, added } -> apply to whitelist_settings.whitelist: added -> push (dedupe already handled in the closure); removed -> remove the entry (by index captured at delete time — carry the removed path in the message; remove by exact path match); status bar: added -> "Added {path} — kills now require confirmation" (status.info); removed -> "Removed {path} — kills are instant again" (status.info); duplicate (added: false, already present) -> "Added"-string must NOT be shown — show "{path} is already on your protection list" (status.info — implementation copy per the flagged assumption; UAT-confirmed). All strings fit 80 cols (Assumption A9 truncation rule: {path} truncates with U+2026 keeping the tail). WhitelistError { path, reason } -> status bar error-tone "Cannot add {path}: {reason}" (UI-SPEC backstop: invalid path -> error, not added). ToggleHelp -> toggle help_active.

7. port-tui/src/components/whitelist_overlay.rs (NEW): stateless WhitelistOverlayComponent rendering the UI-SPEC 20-row layout in the pre-computed area (content_height - 1, Clear-over):
   Row 0: title "Protection Whitelist" (Bold fg_emphasis) + "[Esc]" muted.
   Row 1: "Built-in — system-critical, cannot be terminated" (fg_muted label).
   Rows 2-10 (Length(9)): built-in list — first 9 BUILTIN entries: "◆ {basename}  {short reason}" with ◆ in status.error, name fg_muted Dim (read-only; not focusable per UI-SPEC Focus Management); "System (PID 4)" and "Idle (PID 0)" special-case rows rendered from the PID entries.
   Row 11: "User list — termination requires confirmation" (fg_muted).
   Rows 12-16 (Min(5)): user list — "→ {full executable path}" per entry (U+2192), selected entry Reverse, empty state copy "No user-protected processes. Enter a path below and press Enter to add." (fg_muted); scrollable with the ports.rs render_scrollbar pattern when overflowing.
   Row 17: "Path: >_" input row — search.rs block-cursor pattern verbatim (whitelist_input + whitelist_input_cursor), placeholder "type a path... use absolute paths" muted when empty.
   Row 18: hint "[j/k]Move [d]Delete [Tab]Focus [Enter]Add [Esc]Close".
   Row 19: bottom border.
   Data source: built-in section from port_core::process::BUILTIN (constant, imported via port_core::process::BUILTIN); user section from app.whitelist_settings.whitelist. Long paths truncate U+2026 without wrapping (UI-SPEC overflow rule).

8. port-tui/src/components.rs: add `pub mod whitelist_overlay;` + `pub use whitelist_overlay::WhitelistOverlayComponent;`.

9. port-tui/src/main.rs render_app overlay placement: after the detail panel render (plan 02-02), add `if app.whitelist_active { let wl_area = Rect { height: content_area.height.saturating_sub(1), ..content_area }; WhitelistOverlayComponent.render(app, f, wl_area, theme); }` and after it (topmost, above whitelist — stack order per UI-SPEC: table -> search -> filter -> detail -> whitelist -> confirm) the existing confirm render. Also `if app.help_active` render HelpComponent last-but-confirm (see Task 2).

10. port-tui/src/main.rs footer: add the whitelist-overlay branch: "[j/k]Move [d]Delete [Tab]Focus [Enter]Add [Esc]Close" (locked string, UI-SPEC footer table).
</action>
  <verify>
    <automated>cargo test -p port-core process::whitelist && cargo test --workspace && cargo build -p port-tui</automated>
    <human-check>Run `cargo run -p port-tui`, press w: built-in section shows ◆ entries with reasons; add an existing absolute path (e.g. the current port-tui exe) — "Added" status.info; add a bogus path — error, not added; duplicate add — no-op info string; kill a user-listed process — confirm dialog appears; remove it in the overlay — next kill is instant; press x on a built-in row — hard-block message only.</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test -p port-core process::whitelist` green (validate/normalize matrix); `cargo test --workspace` green
    - w key bound; whitelist overlay dispatch matches the UI-SPEC pass-through table (j/k/d/Tab/BackTab/Enter/Esc/printable/arrows in the right focus states; r/s/tab/quit pass)
    - Add flow: WhitelistAdd -> spawn_blocking validate+save -> WhitelistSaved/WhitelistError; duplicate add leaves settings.toml unchanged (round-trip test proves no duplicates); nonexistent path -> error string, entry not added
    - Delete flow: bounds-checked; WhitelistSaved { added: false } removes by exact path
    - Status-bar strings: "Added {path} — kills now require confirmation", "Removed {path} — kills are instant again", duplicate info string, "Cannot add {path}: {reason}" — all status.info/error tone per UI-SPEC color map, all <=80 cols
    - Overlay: built-in section read-only (no delete/add path into BUILTIN — grep 'BUILTIN' port-tui/src/components/whitelist_overlay.rs shows a read-only render only)
  </acceptance_criteria>
  <done>Users manage their protection list live from the w overlay with validated input and instant effect; the kill gate honors changes on the next attempt; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Help overlay (? key) + SRCH-02 verification + phase traceability</name>
  <read_first>port-tui/src/main.rs (map_key_event 219-317, render_app overlay placement, footer 583-676), port-tui/src/components.rs, port-tui/src/components/overview.rs (placeholder-component pattern), port-core/src/filter.rs (SRCH-02 implementation + existing tests — VERIFY ONLY, no behavior change), .planning/REQUIREMENTS.md (SRCH-02 row), .planning/ROADMAP.md (Phase 2 section), 02-UI-SPEC.md (Help Overlay section, Keyboard Contract)</read_first>
  <files>port-tui/src/components/help.rs, port-tui/src/components.rs, port-tui/src/main.rs, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>Close the phase's documentation surface: the Help overlay (referenced by the Phase 1 footer "[?]Help" and the UI-SPEC Help Overlay section but absent from the codebase — PATTERNS.md no-analog table) and the SRCH-02 traceability closure.

1. port-tui/src/components/help.rs (NEW — first Help component; stateless, overview.rs placeholder style + bordered Block): renders a Clear-over overlay covering the content area (below the confirm dialog in stack order) with sections:
   - "Keys" sections per the UI-SPEC Keyboard Contract: L0 universal (1-5 tabs, Tab/Shift+Tab, Esc semantics), navigation (j/k/up/down/g/G), L2 actions: [d] Detail, [x] Kill, [w] Whitelist, [s] Sort (retained — the Help overlay is the canonical reference for the footer-dropped s and w per UI-SPEC), [r] Refresh, [/] Search, [f] Filter, [a] Elevate (when not admin), L2-confirm: [y] Confirm kill · [n] Cancel, L3 power: [q] Quit.
   - A note line (fg_muted): "Command lines shown in the detail panel may contain secrets — they are displayed locally only and never persisted or exported." (surfaces prohibition P4 + RESEARCH Security Domain note to users).
   - A second note line (fg_muted): "Protection matching is name-based — all instances of a built-in name (e.g. svchost.exe) are protected." (implements threat T-02-06: documents the D-10 heuristic limitation to users; the whitelist overlay keeps its locked 20-row layout, so the note lives here).
   - Esc hint "[Esc] Close".
   Layout: title "Help" Bold fg_emphasis, rows grouped by section with fg_muted labels, hotkey chars in accent_primary (Phase 1 convention: hotkey underlines in footer; in Help use accent_primary for key spans). No new theme slots.

2. port-tui/src/components.rs: add `pub mod help;` + `pub use help::HelpComponent;`.

3. port-tui/src/main.rs: (a) map_key_event: the '?' -> Message::ToggleHelp binding added in Task 1 step 4a — verify it dispatches before the default match (help is a universal overlay); (b) render_app: `if app.help_active { HelpComponent.render(app, f, content_area, theme); }` placed after the whitelist overlay render and before the confirm render (confirm stays topmost); (c) status bar: no change (help has no status interaction).

4. port-tui/src/update.rs: ToggleHelp handler (added in Task 1 step 6 — verify it toggles app.help_active without disturbing other overlay flags).

5. SRCH-02 verification (VERIFICATION ONLY — no filter engine changes; the only filter.rs delta in this phase is the user_protected:false constructor update from plan 02-02): run the existing filter test suite (`cargo test -p port-core filter`) and confirm it exercises AND-across-dimensions / OR-within-Vec-field combinations (filter.rs apply_filters + inline tests). If any test name does not cover the AND/OR combination explicitly, add ONE assertion-level test to the existing filter.rs test module documenting the combination (additive, still no engine change).

6. Traceability updates: (a) .planning/REQUIREMENTS.md: SRCH-02 row (line ~22) `- [ ] **SRCH-02**: ...` -> `- [x] **SRCH-02**: ...`; Traceability table row `| SRCH-02 | Phase 2 | Pending |` -> `| SRCH-02 | Phase 2 | Complete |`; (b) .planning/ROADMAP.md: Phase 2 requirement checkboxes stay (the Requirements line lists IDs), but update the Phase 2 success-criteria progress note if the ROADMAP format tracks them (check the Phase 1 section precedent — Phase 1 rows show `[x]` only in the plan list; mirror whatever Phase 1 did after completion, i.e. mark the plan list items done when execution reports them — for THIS plan mark nothing executed yet; the traceability table in REQUIREMENTS.md is the SRCH-02 closure point).
</action>
  <verify>
    <automated>cargo test -p port-core filter && cargo test --workspace && cargo build -p port-tui && grep -n 'SRCH-02.*Complete' .planning/REQUIREMENTS.md</automated>
    <human-check>Run `cargo run -p port-tui`, press ?: Help overlay lists all keys including d/x/w/s/y/n; Esc closes; press w then ? — help renders above the whitelist overlay (stack order); footer [?]Help works.</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test -p port-core filter` green (SRCH-02 AND/OR combination verified by existing tests; engine untouched)
    - `cargo build -p port-tui` compiles; '?' opens Help, Esc closes; help renders below confirm but above whitelist
    - REQUIREMENTS.md: SRCH-02 checkbox is [x] and the traceability row reads "Complete" (grep 'SRCH-02.*Complete' .planning/REQUIREMENTS.md matches)
    - Help overlay copy includes the d/x/w/s/y/n keys, the command-line-secrets note, and the name-based-matching note (Task 2 step 1) — no other copy invention
  </acceptance_criteria>
  <done>Every keyboard capability is documented in-app; SRCH-02 is verified and traced Complete; phase artifacts closed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 02-03)

New symbols, files, and config this plan creates:

| Kind | Symbol / File | Notes |
|------|---------------|-------|
| fn | `whitelist::normalize_user_entry(&str) -> Option<String>` | Trim/quotes/control-char/absolute-check/trailing-sep; GetLongPathNameW on existing files |
| fn | `whitelist::validate_user_entry(&str) -> Result<String, String>` | UI-SPEC backstop: absolute + existing + length cap 4096 |
| messages | `ToggleWhitelistOverlay, WhitelistFocusNext, WhitelistFocusPrev, WhitelistSelectMove, WhitelistDeleteSelected, WhitelistInput, WhitelistBackspace, WhitelistCursorMove, WhitelistAdd, WhitelistSaved, WhitelistError, ToggleHelp` | |
| enum | `app::WhitelistFocus { List, Input }` with next()/prev() | |
| app fields | `whitelist_active, whitelist_focus, whitelist_selected, whitelist_input, whitelist_input_cursor, whitelist_settings, help_active` | |
| component | `WhitelistOverlayComponent` | 20-row, two lists + input |
| component | `HelpComponent` | '?' overlay, all keys + secrets note |
| docs | REQUIREMENTS.md SRCH-02 -> Complete; traceability row | Verification-only closure |
| tests | validate/normalize matrix in whitelist.rs; optional AND/OR combination assertion in filter.rs tests | |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| w overlay path input (user text) -> validate_user_entry -> settings.toml | Untrusted path text becomes protection policy — the input validator is the gate |
| settings.toml (user-writable) -> save/load cycle -> kill gate | File content directly controls which processes are protected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | whitelist.rs validate/normalize + user_match | high | mitigate | Input validator: absolute-path syntax, control-char rejection, quote strip, trailing-separator strip, 8.3 via GetLongPathNameW, length cap 4096, existence check (UI-SPEC backstop); user_match normalization already locked by plan 02-01 tests; unit tests cover the bypass matrix (prohibition P3) |
| T-02-08 | Tampering | settings.toml TOCTOU (save vs kill re-read) | medium | accept | Single-user local tool; the kill path re-reads settings fresh (D-15) closing the window to <1ms; the file lives in the user's own %APPDATA% — same trust domain as the app |
| T-02-06 | Spoofing | BUILTIN display (whitelist overlay) | medium | accept | Name/PID matching is heuristic for the non-PPL subset (D-10); the Help overlay note documents that matching is name-based — all instances of a built-in name are protected (Task 2 step 1); PPL blocks real critical processes regardless |
| T-02-03 | Information Disclosure | Help overlay note | medium | accept | Command-line secrets are display-only (prohibition P4); the Help overlay surfaces the warning to users (transparency) |
| T-02-SC | Tampering | cargo build | high | mitigate | Zero new crates; only existing locked deps + windows features from plan 02-01 (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `cargo test -p port-core process::whitelist` — validation/normalization matrix (VALIDATION map TBD-02/03 additions)
- `cargo test -p port-core filter` — SRCH-02 verified unchanged (TBD-09)
- `cargo test --workspace` — full suite green before phase gate
- Manual UAT deferred to end-of-phase: whitelist hard-block UX, immediate effect, Help overlay (VALIDATION map manual items)
</verification>

<success_criteria>
- Users add/remove protection entries from the w overlay with validated input and immediate effect; built-in protection is transparent (reasons visible) and immutable
- The ? Help overlay documents every keyboard capability, including the footer-dropped keys
- SRCH-02 is verified by the existing test suite and marked Complete in REQUIREMENTS.md
</success_criteria>

<output>
Create `.planning/phases/02-process-management-smart-kill/02-03-SUMMARY.md` when done
</output>
