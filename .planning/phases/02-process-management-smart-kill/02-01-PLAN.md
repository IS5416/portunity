---
phase: 02-process-management-smart-kill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - port-core/src/process.rs
  - port-core/src/process/handle.rs
  - port-core/src/process/whitelist.rs
  - port-core/src/process/kill.rs
  - port-core/src/config/settings.rs
  - port-core/tests/kill_integration.rs
  - port-core/tests/process_handle_integration.rs
  - port-tui/src/message.rs
  - port-tui/src/app.rs
  - port-tui/src/main.rs
  - port-tui/src/update.rs
  - port-tui/src/components.rs
  - port-tui/src/components/kill_confirm.rs
autonomous: true
requirements: [PROC-01, PROC-02, PROC-03, PROC-04, PROC-07]
must_haves:
  truths:
    - "Pressing x kills the selected row's owning process; a process owning multiple ports is terminated once and all its ports free together after the post-kill scan (PROC-01)"
    - "Pressing x with no selected row is a no-op — no kill, no dialog (PROC-01)"
    - "Repeated x on an already-exited process yields the 'already exited' outcome message; no duplicate-kill attempt (PROC-01)"
    - "Non-protected process: x kills instantly (no dialog); outcome shows in the status bar (PROC-03, D-01, D-04)"
    - "Smart kill: graceful signal first (WM_CLOSE for visible windows, Ctrl+C helper for console), waits kill_timeout_secs (default 5, settings.toml), force-kills via TerminateProcess if unresponsive; process with neither window nor console is terminated directly without a pointless wait (PROC-02, D-02)"
    - "Every kill outcome is displayed in the existing status bar with the 8 locked UI-SPEC strings, each ≤80 cols with tail-preserving truncation; successful kill triggers one immediate scan so the freed port disappears (PROC-02, D-04)"
    - "Built-in whitelist (≥25 entries, Microsoft Restart Manager grounded, each with a plain-language reason) hard-blocks termination: no kill path exists, status bar shows the non-technical explanation and a hint to review the whitelist; check happens BEFORE OpenProcess (PROC-04, D-09, Pitfall #11)"
    - "User-whitelisted processes (full-path match, case-insensitive normalized) gate the kill behind a 60x7 centered confirmation dialog; y/Enter confirms, n/Esc cancels; x is intercepted while the dialog is open (PROC-03, D-09, D-11)"
    - "Kill identity is a Send-safe ProcessSnapshot (PID + creation FILETIME + path); the raw HANDLE is opened, verified (GetProcessId + GetProcessTimes creation time against the snapshot), and acted on inside one spawn_blocking scope; creation-time mismatch aborts the kill — no wrong-process kill (PROC-07, Pitfall #1)"
    - "settings.toml is re-read before every kill attempt (D-15) — a whitelist change takes effect on the next kill without restart"
    - "Access-denied kills (non-admin targeting elevated/system processes) map to the 'admin rights needed — Press a to elevate' outcome; no auto-elevation, no state-loss relaunch (D-03)"
  artifacts:
    - path: "port-core/src/process/handle.rs"
      provides: "ProcessSnapshot (pid, creation_time: Option<FILETIME>, executable_path: Option<String>) — Send-safe process identity crossing the TEA channel; internal OpenProcessHandle with Drop->CloseHandle; open_verified() with creation-time verification"
      exports: ["ProcessSnapshot", "snapshot_for", "open_verified"]
    - path: "port-core/src/process/whitelist.rs"
      provides: "BUILTIN constant (>=25 entries with reasons), builtin_match (basename + PID 0/4), user_match (normalized case-insensitive full path), Protection enum (None/UserConfirm/HardBlocked), protection_status"
      exports: ["BUILTIN", "BuiltinEntry", "builtin_match", "user_match", "Protection", "protection_status"]
    - path: "port-core/src/process/kill.rs"
      provides: "Strategy enum, route_strategy pure fn, KillOutcome enum (8 variants), kill() escalation pipeline in one spawn_blocking"
      exports: ["Strategy", "route_strategy", "KillOutcome", "kill"]
    - path: "port-core/src/config/settings.rs"
      provides: "AppSettings.whitelist: Vec<String> (serde default) + AppSettings.kill_timeout_secs: u64 (serde default 5) — backward compatible with Phase-1-era TOML"
      exports: ["default_kill_timeout_secs"]
    - path: "port-tui/src/components/kill_confirm.rs"
      provides: "KillConfirmComponent — centered 60x7 bordered popup per UI-SPEC"
      exports: ["KillConfirmComponent"]
    - path: "port-core/tests/kill_integration.rs"
      provides: "Windows-gated integration tests: spawn real children, exercise graceful/force/direct/creation-time-mismatch paths"
    - path: "port-core/tests/process_handle_integration.rs"
      provides: "PID-reuse churn test: 10 spawn/kill iterations, no wrong-process kill"
  key_links:
    - from: "port-tui/src/main.rs map_key_event"
      to: "port-tui/src/message.rs"
      via: "x key -> Message::Kill { pid }; confirm layer y/n/Esc/x dispatch"
      pattern: "Message::Kill|KillConfirmed|KillCancelled"
    - from: "port-tui/src/main.rs event loop"
      to: "port-core/src/process/kill.rs"
      via: "spawn_blocking intercept (ElevateRequest template): Kill -> snapshot_for + protection_status -> KillPrepared -> KillExecute/KillConfirmed -> kill() -> KillOutcome"
      pattern: "spawn_blocking|process::kill"
    - from: "port-core/src/process/kill.rs"
      to: "port-core/src/process/handle.rs"
      via: "open_verified() with GetProcessTimes creation-time comparison before TerminateProcess"
      pattern: "open_verified|GetProcessTimes"
    - from: "port-core/src/process/kill.rs"
      to: "port-core/src/process/whitelist.rs"
      via: "protection_status checked BEFORE OpenProcess (Pitfall #11); settings re-read per D-15"
      pattern: "protection_status|builtin_match"
    - from: "port-tui/src/update.rs"
      to: "port-tui/src/app.rs"
      via: "KillOutcome handler sets kill_status + triggers app.scanning = true (D-04 post-kill auto-refresh)"
      pattern: "kill_status|scanning"
  assumptions:
    - "[FLAGGED ASSUMPTION] PROC-02: Escalation routing (WM_CLOSE / Ctrl+C helper / force-direct) is plan-defined per RESEARCH Pattern 2; success criterion 3 requires graceful→timeout→force with the outcome displayed — implemented via route_strategy + the on_timeout progress callback"
    - "[FLAGGED ASSUMPTION] PROC-03: Protection matrix routes x as: instant kill for Protection::None, confirm dialog for Protection::UserConfirm, hard-block status message for Protection::HardBlocked — no dialog for hard-blocked entries (D-09)"
    - "[FLAGGED ASSUMPTION] PROC-04: Built-in whitelist membership (~26-30 entries, Microsoft Restart Manager Critical System Services + session/security tier) — human-verify the exact constant at execution (RESEARCH Assumption A1)"
    - "[FLAGGED ASSUMPTION] PROC-07: ProcessSnapshot (PID + FILETIME creation + path) crosses the channel; raw HANDLE opened/verified/acted on inside one spawn_blocking scope; creation-time FILETIME verified before terminate (RESEARCH Pattern 1)"
  prohibitions:
    - statement: "MUST NOT kill without verifying the process creation FILETIME against the captured ProcessSnapshot (PID reuse must never kill a different process than the user selected)"
    - statement: "MUST NOT perform the FreeConsole/AttachConsole console dance in the TUI process itself — the Ctrl+C helper process is the only place that dance happens (terminal destruction, ERROR_GEN_FAILURE 31)"
    - statement: "MUST NOT report a kill as successful when TerminateProcess failed or when the process is still alive after the force attempt — every failure maps to an explicit KillOutcome (never a false success)"
---

## Phase Goal

**As a** developer or operator, **I want to** inspect the full details of any port's owning process and terminate it with smart-kill escalation and whitelist-gated protection, **so that** I can free ports safely without ever accidentally crashing the system.

> User-story framing note: ROADMAP Phase 2 `**Goal:**` is not in canonical "As a / I want to / so that" form; the story above is a faithful restatement of the approved goal + CONTEXT domain. Consider running `/gsd mvp-phase 2` to canonicalize the ROADMAP line.

<objective>
## Plan 02-01 — Smart kill: core pipeline (tracer) + TUI kill surface

The kill slice of Phase 2. The tracer task proves the phase's riskiest architecture end-to-end at the core level — a real child process terminated through the full escalation pipeline (protection gate → open+verify → graceful → timeout → force) with PID-reuse verification — then the expansion task wires the TUI surface (`x` key, confirmation dialog, status bar outcomes, auto-refresh) using the proven Phase 1 ElevateRequest pattern.

Purpose: PROC-01/02/03/04/07 all hinge on `kill.rs` + `handle.rs` + `whitelist.rs` being correct under real Windows semantics. Proving that chain with a real spawned child before any TUI work isolates Windows-specific failure modes (UIPI, console attachment, PID recycling) in one task.
Output: working `x`-key kill with protection gating, confirmation dialog, and status-bar outcomes — the phase's central capability.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-management-smart-kill/02-CONTEXT.md
@.planning/phases/02-process-management-smart-kill/02-RESEARCH.md
@.planning/phases/02-process-management-smart-kill/02-UI-SPEC.md
@.planning/phases/02-process-management-smart-kill/02-PATTERNS.md

# Source files to read before modifying (all patterns verified in PATTERNS.md with line numbers):
#   port-core/src/process.rs          — trait stub being reshaped (no implementors exist — verified)
#   port-core/src/config/settings.rs  — serde-default field pattern, default_settings(), save_settings()
#   port-core/src/scanner/tcp.rs      — spawn_blocking + error-mapping style (lines 345-403)
#   port-core/src/lib.rs              — Error enum (Platform/NotFound/PermissionDenied/Io)
#   port-tui/src/main.rs              — map_key_event (219-317), ElevateRequest intercept (156-179), status bar (483-580), footer (583-676)
#   port-tui/src/app.rs               — App struct + App::new() exhaustive literal
#   port-tui/src/message.rs           — Message enum
#   port-tui/src/update.rs            — handler block structure, ElevateRequest no-op idiom (210-214)

<interfaces>
Existing contracts the executor builds against (do not re-explore):

From port-core/src/lib.rs:
    pub enum Error { Platform(String), NotFound(String), PermissionDenied(String), Io(#[from] std::io::Error) }
    pub type Result<T> = std::result::Result<T, Error>;

From port-core/src/process.rs (current stub — reshape in Task 1):
    pub trait ProcessManager {
        fn details(&self, pid: u32) -> crate::Result<crate::models::ProcessInfo>;
        fn terminate(&self, pid: u32, force: bool) -> crate::Result<()>;
    }

From port-core/src/config/settings.rs:
    #[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
    pub struct AppSettings { pub admin_detected: bool, pub schema_version: i32 }   // + whitelist + kill_timeout_secs in Task 1
    pub fn settings_path() -> PathBuf; pub fn load_settings() -> crate::Result<AppSettings>; pub fn save_settings(&AppSettings) -> crate::Result<()>

From port-tui/src/message.rs (Message enum — extend in Task 2):
    existing variants: Quit, Tick, Refresh, ScanComplete(Vec<Connection>), ScanError(String), Sort(SortColumn),
    MoveUp, MoveDown, ScrollTop, ScrollBottom, Search*, Filter*, AdminCheck(bool), ElevateRequest, ElevateDeclined, SwitchTab(usize)

From port-tui/src/app.rs (App — extend in Task 2):
    pub ports: Vec<Connection>, pub filtered_ports: Vec<Connection>, pub scanning: bool, pub error: Option<String>,
    pub selected_index: usize, pub is_admin: bool, pub admin_check_done: bool, pub elevating: bool, pub active_tab: usize,
    pub display_data() -> &[Connection], App::new() (exhaustive struct literal — every new field must be initialized)

windows-rs 0.62.2 signatures (verified against local cargo registry — do NOT use 0.73):
    OpenProcess(desired_access: PROCESS_ACCESS_RIGHTS, inherit: bool, pid: u32) -> Result<HANDLE>
    TerminateProcess(h: HANDLE, exit_code: u32) -> Result<()>
    GetProcessTimes(h: HANDLE, creation: *mut FILETIME, exit: *mut FILETIME, kernel: *mut FILETIME, user: *mut FILETIME) -> Result<()>
    GetProcessId(h: HANDLE) -> u32; GetExitCodeProcess(h: HANDLE, code: *mut u32) -> Result<()>
    WaitForSingleObject(h: HANDLE, ms: u32) -> WAIT_EVENT  (WAIT_OBJECT_0 = 0, WAIT_TIMEOUT = 258)
    QueryFullProcessImageNameW(h: HANDLE, flags: u32, buffer: &mut [u16], size: *mut u32) -> Result<()>
    EnumWindows(cb: WNDENUMPROC, lparam: LPARAM) -> Result<()>; GetWindowThreadProcessId(hwnd: HWND, pid: Option<*mut u32>) -> u32
    PostMessageW(hwnd: Option<HWND>, msg: u32, wparam: WPARAM, lparam: LPARAM) -> Result<()>; WM_CLOSE: u32 = 16
    AttachConsole(pid: u32) -> Result<()>; FreeConsole() -> Result<()>; GenerateConsoleCtrlEvent(ctrl: u32, group: u32) -> Result<()>
    SetConsoleCtrlHandler(handler: Option<unsafe extern "system" fn(u32) -> i32>, add: bool) -> Result<()>; CTRL_C_EVENT: u32 = 0
    PROCESS_QUERY_LIMITED_INFORMATION | PROCESS_TERMINATE | PROCESS_SYNCHRONIZE  (0x1000 | 0x1 | 0x100000)
    HANDLE is !Send (repr(transparent) *mut c_void) — never stored in App state or sent over mpsc; ProcessSnapshot is the channel payload
    STILL_ACTIVE: u32 = 259; ERROR_ACCESS_DENIED: u32 = 5; ERROR_INVALID_PARAMETER: u32 = 87
</interfaces>

<assumption_delta_decision>
ROADMAP success criterion 3 says "waits a configurable timeout" — the assumption-delta detector flagged the term `configurable`. Resolution: NO DELTA — CONTEXT D-02 already chose the configuration mechanism (`kill_timeout_secs` in settings.toml, default 5). The term is ADDED-ALONGSIDE the existing D-02 decision, not promoted to a new decision; nothing new to choose. Implemented in Task 1 (settings.rs field) and honored in Task 2 (TUI reads settings.kill_timeout_secs at kill time).
</assumption_delta_decision>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Terminate a spawned child process through the full escalation pipeline (core tracer)</name>
  <reversibility rating="costly">Implements D-09 (hard-block vs confirm model — switching to unified confirm-and-kill later changes the kill flow, copy, and tests), D-10 (basename-vs-full-path match semantics — a stored-data contract; changing later would silently protect the wrong instances), D-13 (settings.toml whitelist storage — later migration to another store needs a settings migration plus GUI adaptation in Phases 5/6). All three were explicitly user-selected in CONTEXT; ratings are carried for the record only — no checkpoint (already-decided doors).</reversibility>
  <read_first>port-core/src/process.rs, port-core/src/config/settings.rs, port-core/src/scanner/tcp.rs (spawn_blocking + Error::Platform mapping style), port-core/src/scanner/resolver.rs (PID 0/4 special-case pattern), port-core/src/filter.rs (inline #[cfg(test)] module pattern, lines 123-259), port-core/src/lib.rs, Cargo.toml (workspace windows feature list lines 21-32), 02-RESEARCH.md (Patterns 1-3, Code Examples 1-6, Pitfalls 1/2/4/7)</read_first>
  <files>Cargo.toml, port-core/src/process.rs, port-core/src/config/settings.rs, port-core/src/process/handle.rs, port-core/src/process/whitelist.rs, port-core/src/process/kill.rs, port-core/tests/kill_integration.rs, port-core/tests/process_handle_integration.rs</files>
  <behavior>
    - route_strategy(has_visible_windows, has_console) maps (true,_)->WmClose, (false,true)->ConsoleCtrlC, (false,false)->ForceDirect
    - protection_status: built-in basename/PID match -> HardBlocked(reason); user full-path match -> UserConfirm; else None; built-in tier checked FIRST
    - builtin_match: PID 0 -> "Idle", PID 4 -> "System", lowercase basename compare against BUILTIN; returns the plain-language reason
    - user_match: normalized case-insensitive full-path compare; "C:\PROGRA~1\x\a.exe" (8.3) matches "C:\Program Files\x\a.exe" after GetLongPathNameW; quotes/trailing separators stripped
    - BUILTIN contract: >=25 entries, unique lowercase names, non-empty reason per entry
    - creation-time verify: GetProcessTimes creation FILETIME mismatch vs snapshot -> abort (NotFound)
    - integration: spawned console child (cmd.exe /c ping -t 127.0.0.1, CREATE_NEW_CONSOLE) dies via kill() within timeout; signal-ignoring child (CREATE_NEW_PROCESS_GROUP) -> ForceKilled; wrong-creation-time snapshot -> Failed + child survives
  </behavior>
  <action>Build the entire core kill stack as one vertical slice. All Win32 calls run inside tokio::task::spawn_blocking (Pitfall #9); HANDLE never crosses an async boundary (windows-rs 0.62 HANDLE is !Send — verified); every failure maps to crate::Error (lib.rs variants) or KillOutcome — never swallowed (Pitfall #11). New-style module layout per CLAUDE.md: leaf modules process/handle.rs, process/whitelist.rs, process/kill.rs (no mod.rs files).

1. Cargo.toml (workspace, lines 21-32): append exactly two entries to the existing windows 0.62 feature list: "Win32_Security_WinTrust" (WinVerifyTrustEx — used in plan 02-02) and "Win32_System_Console" (AttachConsole/FreeConsole/GenerateConsoleCtrlEvent/SetConsoleCtrlHandler). Zero new crates — this is the phase's entire dependency delta.

2. port-core/src/config/settings.rs (per D-02, D-13): add `#[serde(default)] pub whitelist: Vec<String>` and `#[serde(default = "default_kill_timeout_secs")] pub kill_timeout_secs: u64` to AppSettings; add fn default_kill_timeout_secs() -> u64 { 5 }; add both fields to default_settings(). Add an inline #[cfg(test)] module (filter.rs precedent): (a) serde round-trip of a Phase-1-era TOML fixture (admin_detected + schema_version only) proving the two new fields default correctly; (b) round-trip with a populated whitelist preserving order and duplicates-as-written.

3. port-core/src/process.rs (trait reshape per RESEARCH + CORE-02): replace the stub with `pub mod handle; pub mod whitelist; pub mod kill;` + re-exports (pub use handle::ProcessSnapshot; pub use whitelist::{Protection, BUILTIN, builtin_match, user_match, protection_status}; pub use kill::{KillOutcome, Strategy, route_strategy, kill}). Reshape the ProcessManager trait (no implementors exist — safe): `async fn details(&self, snapshot: &ProcessSnapshot) -> crate::Result<crate::models::ProcessInfo>` and `async fn terminate(&self, snapshot: &ProcessSnapshot, timeout_secs: u64, on_timeout: ...) -> KillOutcome` — hmm, trait terminate does not need the progress callback (that is TUI copy); give the trait `async fn terminate(&self, snapshot: &ProcessSnapshot, timeout_secs: u64) -> KillOutcome`. Implement `pub struct WindowsProcessManager;` in process.rs delegating to the free functions (thin wrapper, mirrors WindowsPortScanner delegation in windows.rs). Do NOT declare `pub mod info;` yet — info.rs lands in plan 02-02; declaring it now breaks compilation.

4. port-core/src/process/handle.rs (PROC-07, RESEARCH Pattern 1): `#[derive(Debug, Clone)] pub struct ProcessSnapshot { pub pid: u32, pub creation_time: Option<windows::Win32::Foundation::FILETIME>, pub executable_path: Option<String> }`. Internal `struct OpenProcessHandle { pid: u32, handle: HANDLE }` with `impl Drop { CloseHandle }` — never crosses a channel. Private `fn open_with(pid: u32, rights: PROCESS_ACCESS_RIGHTS) -> crate::Result<OpenProcessHandle>` using const RIGHTS = PROCESS_QUERY_LIMITED_INFORMATION | PROCESS_TERMINATE | PROCESS_SYNCHRONIZE. `pub fn snapshot_for(pid: u32) -> crate::Result<ProcessSnapshot>`: open with RIGHTS minus PROCESS_TERMINATE (QLI|SYNCHRONIZE), GetProcessTimes creation FILETIME, QueryFullProcessImageNameW into a 32KiB u16 buffer (retry once with 64KiB if ERROR_INSUFFICIENT_BUFFER... the canonical call is size-in/out: pass capacity, read back needed size, resize and retry), close, return snapshot. `pub fn open_verified(snapshot: &ProcessSnapshot) -> crate::Result<OpenProcessHandle>`: open with RIGHTS, `if GetProcessId(h.handle) != snapshot.pid -> Err(NotFound("pid reused"))`; if snapshot.creation_time is Some(ct): GetProcessTimes into local FILETIME, compare the two u64 bit-patterns (dwLowDateTime | dwHighDateTime<<32) — mismatch -> Err(NotFound("process replaced (creation time mismatch)")). Inline #[cfg(test)]: pure FILETIME-compare helper tests (extract the comparison into a pure fn `creation_matches(actual: FILETIME, expected: FILETIME) -> bool` for testability — handles None-when-scan-derived rows by skipping).

5. port-core/src/process/whitelist.rs (PROC-03/04, D-09/D-10, D-12; filter.rs free-function precedent — no trait): `pub struct BuiltinEntry { pub name: &'static str, pub reason: &'static str }`. `pub const BUILTIN: &[BuiltinEntry]` — 26-30 entries per RESEARCH Assumption A1, grounded in Microsoft Restart Manager Critical System Services + session/security tier: Tier-1: smss.exe, csrss.exe, wininit.exe, winlogon.exe, services.exe, lsass.exe, svchost.exe, logonui.exe, dwm.exe, fontdrvhost.exe, Memory Compression, Registry, System (PID 4), Idle (PID 0); Tier-2: lsaiso.exe, sessionenv.exe, conhost.exe, spoolsv.exe, audiodg.exe, MsMpEng.exe, SgrmBroker.exe, plus any additional entries the executor grounds in the Restart Manager list to reach ~26-30 — every entry carries a short plain-language reason (e.g. "Local security authority", "Windows kernel"). `pub fn builtin_match(pid: u32, basename: &str) -> Option<&'static str>`: PID 0 -> "Idle" reason, PID 4 -> "Windows kernel" reason; else lowercase-compare basename against BUILTIN names. `pub fn user_match(path: &str, entries: &[String]) -> bool`: normalize path (trim, strip surrounding quotes, strip trailing '\' or '/'), compare with eq_ignore_ascii_case against each normalized entry. `pub enum Protection { None, UserConfirm, HardBlocked(&'static str) }`. `pub fn protection_status(pid: u32, basename: &str, path: Option<&str>, settings: &AppSettings) -> Protection`: built-in tier FIRST (Pitfall 6 — user tier only consulted when builtin_match is None), user tier needs a Some(path). Inline #[cfg(test)] locking the PROC-04/PROC-03 contracts: BUILTIN >=25 entries, names unique + lowercase, every reason non-empty; builtin_match PID 0/4 special cases + basename case-insensitivity; protection_status matrix (built-in / user-listed / neither / built-in AND user-listed -> HardBlocked wins); user_match normalization matrix (quotes, case, trailing separator, 8.3-style variants via pre-normalized inputs).

6. port-core/src/process/kill.rs (PROC-02, RESEARCH Pattern 2): 
   - `pub enum Strategy { WmClose, ConsoleCtrlC, ForceDirect }` and pure `pub fn route_strategy(has_visible_windows: bool, has_console: bool) -> Strategy` (the (false,false) -> ForceDirect branch is the "no pointless 5s wait" rule from Claude's Discretion).
   - `pub enum KillOutcome { Graceful, ForceKilled, Direct, AlreadyExited, AccessDenied, HardBlocked(&'static str), Failed(String) }`.
   - `pub async fn kill(snapshot: ProcessSnapshot, timeout_secs: u64, on_timeout: impl FnOnce() + Send + 'static) -> KillOutcome`: single spawn_blocking scope executing steps 1-8 of RESEARCH Pattern 2: (1) load_settings() fresh (D-15) + protection_status(snapshot.pid, basename_from(snapshot.executable_path), snapshot.executable_path.as_deref(), &settings) — HardBlocked(reason) returned WITHOUT any OpenProcess (Pitfall #11); (2) open_verified(&snapshot) — NotFound -> Failed("process replaced (creation time mismatch)"); (3) GetExitCodeProcess != STILL_ACTIVE -> AlreadyExited (drop handle, done); (4) probe visible windows: EnumWindows collecting HWNDs whose GetWindowThreadProcessId == pid, prefer visible top-level ones (GetWindowLong/IsWindowVisible for the visible filter) — any found -> PostMessageW(hwnd, WM_CLOSE, 0, 0) on the first visible window (fire-and-forget; UIPI silently blocks cross-integrity — by design, becomes timeout->force->AccessDenied per Pitfall 3) -> Strategy::WmClose; (5) else console probe: AttachConsole(pid) succeeds -> FreeConsole() immediately (this is a PROBE in the kill scope; the TUI process itself never calls FreeConsole — prohibition P2) -> Strategy::ConsoleCtrlC; (6) else Strategy::ForceDirect; (7) WaitForSingleObject(handle, timeout_secs * 1000): WAIT_OBJECT_0 -> Graceful; WAIT_TIMEOUT -> invoke on_timeout() (TUI copy hook — "Graceful close timed out" status), then TerminateProcess(handle, 1) -> WaitForSingleObject(handle, 3000) -> WAIT_OBJECT_0 -> ForceKilled, else Failed("process did not exit after force kill") (Assumption A5); (8) map errors: ERROR_ACCESS_DENIED from OpenProcess/TerminateProcess -> AccessDenied (D-03); AttachConsole ERROR_INVALID_PARAMETER/ERROR_ACCESS_DENIED -> treated as "no console" (console-less process -> not an error); other OS errors -> Failed(reason). The ConsoleCtrlC strategy spawns the helper: Command::new(std::env::current_exe()?).arg("--ctrl-c").arg(pid.to_string()).creation_flags(0x08000000 /* CREATE_NO_WINDOW */).status() — exit 0 means delivered (document caveats in a comment: event broadcasts to all processes on that console; CREATE_NEW_PROCESS_GROUP children ignore Ctrl+C; ReadConsole-blocked threads may not interrupt — WaitForSingleObject timeout is the real arbiter, Pitfall 4). Inline #[cfg(test)]: route_strategy full matrix; the outcome-string mapping stays in the TUI (Task 2).
   - Ctrl+C helper mode lives in port-tui (Task 2's main.rs), but the exit-code contract (0 delivered / 1 no console) is documented here.

7. port-core/tests/kill_integration.rs (NEW integration test crate — first tests/ file in the repo; top-level #[test] fns, no #[cfg(test)] needed): Windows-only tests that spawn real children and exercise the pipeline:
   - test_graceful_console_child: spawn `cmd.exe /c ping -t 127.0.0.1` with creation_flags CREATE_NEW_CONSOLE (0x00000010 — CRITICAL: the child must own its own console so the helper's Ctrl+C broadcast cannot reach the test harness; if spawned without it, GenerateConsoleCtrlEvent would signal the test process too). Capture snapshot_for(child_pid), call kill(snapshot, 2, noop). Assert outcome is Graceful or ForceKilled and the child exits within 5s (sysinfo::System::refresh_processes + exists(pid) poll, or GetExitCodeProcess != STILL_ACTIVE).
   - test_timeout_then_force: spawn a signal-ignoring console child (creation_flags CREATE_NEW_PROCESS_GROUP | CREATE_NEW_CONSOLE — new process group ignores Ctrl+C), kill(snapshot, 1, noop) -> assert ForceKilled and child exited.
   - test_creation_time_mismatch_aborts: spawn child, capture snapshot, then build a SECOND snapshot with a corrupted creation_time (flip one FILETIME word) -> kill() -> assert Failed (or the NotFound mapping) AND the child is still alive afterwards (GetExitCodeProcess == STILL_ACTIVE) — the wrong-process-kill protection proof.
   - test_already_exited: spawn a quick-exiting child (cmd.exe /c exit), wait for exit, capture snapshot BEFORE exit if possible (race-tolerant: snapshot_for right after spawn; if the process already exited, skip-assert the AlreadyExited path via a snapshot of a PID that is gone -> AlreadyExited or Failed — assert the outcome is NOT Graceful/ForceKilled and no exception).
   - test_access_denied_path: kill a system-owned process PID (e.g., PID 4 System) — expect HardBlocked (never reaches OpenProcess). Do NOT attempt a real access-denied TerminateProcess in tests (UIPI/PPL varies) — that path is covered by unit mapping + manual UAT.
8. port-core/tests/process_handle_integration.rs (PROC-07): churn test — 10 iterations: spawn `cmd.exe /c timeout 30`, snapshot_for(pid), verify open_verified(snapshot) Ok; then kill the child; then assert open_verified on a snapshot with a stale/forged creation_time Err(NotFound). Assert the verification logic never returns Ok for a wrong identity.
</action>
  <verify>
    <automated>cargo test -p port-core process::whitelist process::kill process::handle config::settings -x && cargo test -p port-core --test kill_integration --test process_handle_integration</automated>
    <human-check>Review BUILTIN constant (Assumption A1): every entry is a real system-critical Windows process with a plain-language reason; Tier-1 matches the Microsoft Restart Manager Critical System Services list.</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test -p port-core` passes with the new inline modules green: whitelist contract (>=25 entries, unique lowercase names, non-empty reasons, PID 0/4, built-in-before-user), route_strategy matrix, creation_matches pure logic, settings serde defaults on a Phase-1-era TOML fixture
    - `cargo test -p port-core --test kill_integration` passes: real spawned children are terminated via the core API (graceful or force), creation-time mismatch aborts with the child surviving, HardBlocked for System (PID 4)
    - `cargo test -p port-core --test process_handle_integration` passes: 10-iteration churn with no wrong-identity accept
    - `cargo build --workspace` compiles (windows features added; process.rs declares handle/whitelist/kill only — no info module yet)
    - grep -v '^#' Cargo.toml | grep -c 'Win32_Security_WinTrust' == 1 and grep -v '^#' Cargo.toml | grep -c 'Win32_System_Console' == 1
    - No HANDLE type appears in any mpsc message payload or App state (grep 'HANDLE' port-tui/src/ returns nothing; in port-core, HANDLE only inside handle.rs)
  </acceptance_criteria>
  <done>The core kill pipeline terminates real child processes through the full escalation chain with PID-reuse verification and protection gating; integration + unit tests green; committed as one tracer commit.</done>
</task>

<task type="auto">
  <name>Task 2: x key kills the selected row's process — confirm dialog, status bar outcomes, auto-refresh (TUI)</name>
  <reversibility rating="costly">Implements D-03 (prompt + 'a' elevation, no auto-elevate — adding auto-elevate later requires a --kill-pid CLI arg, a relaunch protocol, and a handoff path between processes). User-selected in CONTEXT; rating carried for the record — no checkpoint.</reversibility>
  <read_first>port-tui/src/main.rs (map_key_event 219-317, ElevateRequest intercept 156-179, status bar 483-580, footer 583-676), port-tui/src/app.rs (App struct + App::new), port-tui/src/message.rs, port-tui/src/update.rs (ElevateRequest no-op idiom 210-214), port-tui/src/components.rs, port-tui/src/components/filter_panel.rs (Clear-over overlay + component pattern), port-tui/src/theme.rs (accent_secondary exists line 22), 02-UI-SPEC.md (Kill Flow Copy table, Confirm dialog geometry/copy, footer locked strings, Keyboard Contract L2/L2-confirm, Message Additions, Assumption A9 truncation), 02-PATTERNS.md (main.rs/app.rs/message.rs/update.rs sections)</read_first>
  <files>port-tui/src/message.rs, port-tui/src/app.rs, port-tui/src/main.rs, port-tui/src/update.rs, port-tui/src/components.rs, port-tui/src/components/kill_confirm.rs</files>
  <action>Wire the TUI kill surface, replicating the Phase 1 search/filter overlay + ElevateRequest patterns exactly. Flow (two-stage protection-gated kill, D-09/D-15):

1. port-tui/src/message.rs: add `Kill { pid: u32 }`, `KillPrepared { snapshot: ProcessSnapshot, protection: Protection, name: String, pid: u32 }`, `KillConfirmed { pid: u32 }`, `KillCancelled`, `KillStart { name: String, pid: u32 }`, `KillTimeout { name: String, pid: u32, timeout_secs: u64 }`, `KillExecute { snapshot: ProcessSnapshot }`, `KillOutcome { outcome: KillOutcome, name: String, pid: u32 }`. Note in a doc comment: KillPrepared/KillExecute are internal flow messages beyond the UI-SPEC list (the UI-SPEC list covers user-facing messages; the two-stage gate needs the extra hops). Import `port_core::process::{KillOutcome, Protection, ProcessSnapshot, kill, snapshot_for, protection_status}` where needed (through the process.rs re-exports).

2. port-tui/src/app.rs: add fields (all initialized in App::new()): `pub kill_status: Option<KillStatus>` where `pub struct KillStatus { pub text: String, pub tone: KillTone }` and `pub enum KillTone { InProgress, Success, Error }` (both types defined in app.rs); `pub kill_in_flight: bool` (mirrors elevating guard — prevents double-kill); `pub confirm_pid: Option<u32>`; `pub confirm_name: Option<String>`; `pub confirm_port: Option<u16>` (from the selected row — needed by the dialog reason string "the program using port {port}"); `pub pending_kill_snapshot: Option<ProcessSnapshot>`; `pub last_killed_pid: Option<u32>` (cleared on ScanComplete; drives ports.rs strikethrough in plan 02-02). Add pure helper `pub fn format_kill_status(name: &str, pid: u32, outcome: &KillOutcome, timeout_secs: u64, term_width: u16) -> (String, KillTone)` mapping to the 8 locked UI-SPEC strings (Kill Flow Copy table): Graceful -> "✓ {name} (PID {pid}) terminated gracefully" / Success; ForceKilled -> "✓ {name} (PID {pid}) force-killed" / Success; Direct -> "✓ {name} (PID {pid}) terminated" / Success; AlreadyExited -> "✗ {name} (PID {pid}) already exited" / Error; AccessDenied -> "✗ Cannot terminate {name} (PID {pid}) — admin rights needed. Press a to elevate." / Error; HardBlocked -> "✗ {name} is protected — it is critical to Windows. Killing it would crash or destabilize your system. Press w to review the whitelist." / Error; Failed(reason) -> "✗ Failed to terminate {name} (PID {pid}): {reason}" / Error. Truncation rule (Assumption A9 — declare it here): every string must render <= term_width; when a string exceeds term_width, truncate {name} with U+2026 and keep the ACTIONABLE TAIL verbatim ("Press w to review the whitelist." / "Press a to elevate."); the HardBlocked string specifically: full form when it fits, else the compact form "✗ {name} … Press w to review the whitelist." with {name} budget = term_width - 41; never wrap, never overflow the status bar. Write #[cfg(test)] unit tests for format_kill_status: all 8 mappings, truncation at 80 cols, tail preservation.

3. port-tui/src/main.rs map_key_event: (a) DEFAULT-mode additions: `KeyCode::Char('x')` -> if app.selected_index maps to a real row in app.display_data() -> Some(Message::Kill { pid }) else None (no-op on empty list — PROC-01 edge truth); (b) CONFIRM layer dispatch inserted BEFORE the default-mode match (after search/filter blocks): `if app.confirm_pid.is_some() { match key.code { 'y'|Enter -> KillConfirmed, 'n'|Esc -> KillCancelled, 'x' -> None /* no-op: prevents re-triggering kill while dialog open (UI-SPEC) */, _ -> {} } }` — pass-through of all other keys per UI-SPEC confirm pass-through table. The k->MoveUp binding at line 281 stays untouched (D-01).

4. port-tui/src/main.rs event loop (ElevateRequest template, lines 156-179): (a) inbound intercept on Message::Kill: guard `!app.kill_in_flight && app.confirm_pid.is_none()`; spawn_blocking: `let snap = snapshot_for(pid)` -> on Err(NotFound) send KillOutcome{outcome: AlreadyExited,...}; else `let settings = load_settings(); let prot = protection_status(snap.pid, basename_from_row, snap.executable_path.as_deref(), &settings)` -> send KillPrepared { snapshot: snap, protection: prot, name: row_process_name, pid }. (b) inbound intercept on Message::KillConfirmed: guard app.pending_kill_snapshot.is_some() && !app.kill_in_flight; take() the pending snapshot, set kill_in_flight = true, read timeout = load_settings().kill_timeout_secs; spawn_blocking: send KillStart first, then `let out = kill(snapshot, timeout, move || { tx.send(KillTimeout{...}) })` -> send KillOutcome { outcome: out, ... }. (c) drain loop extension (lines 184-190, mirroring the ScanComplete special-case): when try_recv returns KillPrepared: if protection == Protection::None -> update() must NOT be called for it; instead spawn the kill task directly (same body as KillConfirmed path, using the incoming snapshot; guarded by kill_in_flight) — instant-kill path; else -> update(app, KillPrepared) (UserConfirm -> dialog state; HardBlocked -> status message). Update the existing `if matches!(msg, Message::ScanComplete(_))` block to also clear app.last_killed_pid on ScanComplete.

5. port-tui/src/update.rs handlers (mutate state only; the no-op-comment idiom from ElevateRequest at 210-214 for intercept-owned messages): Kill -> no-op comment; KillPrepared -> UserConfirm: set confirm_pid/confirm_name/confirm_port (from selected row) + pending_kill_snapshot; HardBlocked(reason) -> kill_status = (formatted hard-block string, Error) — reason from the enum, name from the message; KillConfirmed -> no-op comment (intercept-owned); KillCancelled -> clear confirm_pid/pending_kill_snapshot; KillStart -> kill_status = ("Terminating {name} (PID {pid}) — sending graceful close…", InProgress); KillTimeout -> kill_status = ("Graceful close timed out ({timeout_secs}s) — force killing {name} (PID {pid})…", InProgress); KillExecute -> no-op (intercept-owned); KillOutcome -> kill_status = format_kill_status(...), clear kill_in_flight + confirm state, on Success/Error tones set app.last_killed_pid = Some(pid) when outcome is Graceful/ForceKilled/Direct, and trigger D-04 post-kill auto-refresh: app.scanning = true (the existing scan-spawn logic in the loop picks it up).

6. port-tui/src/main.rs render_app (lines 395-431 area): overlay stack per UI-SPEC (table -> search -> filter -> ... -> confirm LAST, topmost): after the existing search/filter overlay renders, add `if app.confirm_pid.is_some() { let w = content_area.width; let h = content_area.height; let confirm_area = Rect { x: content_area.x + (w - 60) / 2, y: content_area.y + (h - 7) / 2, width: 60.min(w), height: 7.min(h) }; KillConfirmComponent.render(app, f, confirm_area, theme); }` — centered 60x7 popup, always topmost.

7. port-tui/src/components/kill_confirm.rs (NEW, filter_panel.rs pattern): stateless renderer; render_widget does Clear over the area, then a bordered Block (Borders::ALL, style bg_overlay, title "Confirm termination" per UI-SPEC Confirm dialog copy) containing: row 1 "{name} (PID {pid})" — {name} truncates with U+2026 to (area.width - 13) chars; row 2 the reason "{name} is on your protection list. Terminating it will stop the program using port {port}." (plain language per UI-SPEC; {port} from app.confirm_port, fallback "?" if None); row 3 buttons: "[y] Confirm kill" with accent_secondary + Underline, " · " separator, "[n] Cancel" muted + Underline (UI-SPEC accent split — accent_secondary for kill-action highlights ONLY). All copy per the Copywriting Contract table; nothing else.

8. port-tui/src/components.rs: add `pub mod kill_confirm;` + `pub use kill_confirm::KillConfirmComponent;` in the existing registration block (lines 9-23).

9. port-tui/src/main.rs footer (lines 640-676): replace the Ports-tab default footer with the UI-SPEC locked string — spans: [jk]Move, [/]Search, [f]Filter, [d]Detail, [x]Kill, [r]Refresh, [q]Quit, [?]Help — 73 cols; this REMOVES [s]Sort and the conditional [a]Elevate from the Ports footer (D-09 status-bar admin indicator covers elevation; s/w stay bound and are documented in the Help overlay which plan 02-03 creates). Add the confirm-dialog footer branch: "[y]Confirm kill [n]Cancel  —  {name} is on your protection list" with {name} truncated with U+2026 to (term_width - 63) chars (UI-SPEC footer table + truncation rule).

10. port-tui/src/main.rs --ctrl-c helper mode (REQUIRED for the ConsoleCtrlC strategy to work — kill.rs spawns current_exe with --ctrl-c): (a) add to the existing clap derive struct (port-tui already depends on clap 4.6): `#[arg(long, hide = true)] ctrl_c_pid: Option<u32>`; (b) at the VERY TOP of main(), before terminal init / raw mode / event loop: `if let Some(pid) = args.ctrl_c_pid { let code = helper_send_ctrl_c(pid); std::process::exit(code); }` — Pitfall 7 guard (helper mode must never reach the UI); (c) `fn helper_send_ctrl_c(pid: u32) -> i32` implementing RESEARCH Pattern 3: SetConsoleCtrlHandler(None, true) (ignore CTRL_C in the helper), FreeConsole(), AttachConsole(pid) — Ok -> GenerateConsoleCtrlEvent(CTRL_C_EVENT, 0) -> exit 0; Err (ERROR_INVALID_PARAMETER 87 = no console) -> exit 1. The exit-code contract (0 delivered / 1 no console) is documented in kill.rs Task 1 step 6.
</action>
  <verify>
    <automated>cargo test --workspace && cargo build -p port-tui</automated>
    <human-check>Run `cargo run -p port-tui` (admin or non-admin): select a non-protected row and press x — status bar shows the graceful/force outcome and the row disappears after refresh; select an svchost.exe/system row and press x — hard-block message with "Press w to review the whitelist." shows and no dialog appears; kill a process twice — second attempt shows "already exited".</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` green (format_kill_status unit tests included)
    - `cargo build -p port-tui` compiles; x key handling present in map_key_event default mode, y/n/Esc/x dispatch in the confirm layer, and the `--ctrl-c <pid>` clap flag + helper guard at the top of main()
    - app.rs contains every new field initialized in App::new(); message.rs contains the 8 new variants with ProcessSnapshot imported from port_core::process
    - The Kill flow is two-stage: Kill -> snapshot_for+protection_status -> KillPrepared -> (None: kill directly | UserConfirm: dialog -> KillConfirmed -> kill | HardBlocked: status message only)
    - No HANDLE/sysinfo handle type in any message payload (grep 'HANDLE' port-tui/src/ returns nothing)
    - Ports-tab footer renders the exact 73-col UI-SPEC string; confirm footer renders "[y]Confirm kill [n]Cancel"
  </acceptance_criteria>
  <done>x kills with full protection routing (instant / confirm dialog / hard block), every outcome shows in the status bar within 80 cols, a successful kill auto-triggers a scan; workspace tests green.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 02-01)

New symbols, files, and config this plan creates:

| Kind | Symbol / File | Notes |
|------|---------------|-------|
| struct | `ProcessSnapshot { pid, creation_time: Option<FILETIME>, executable_path: Option<String> }` | Send-safe channel payload (PROC-07) |
| struct (internal) | `OpenProcessHandle { pid, handle }` with `Drop -> CloseHandle` | Never crosses async boundaries |
| fn | `handle::snapshot_for(pid) -> Result<ProcessSnapshot>` | QLI|SYNCHRONIZE open, creation time + path capture |
| fn | `handle::open_verified(&snapshot) -> Result<OpenProcessHandle>` | GetProcessId + GetProcessTimes verification |
| fn | `handle::creation_matches(actual, expected) -> bool` | Pure, unit-tested |
| const | `handle::RIGHTS = PROCESS_QUERY_LIMITED_INFORMATION \| PROCESS_TERMINATE \| PROCESS_SYNCHRONIZE` | Minimal rights (AV/EDR posture) |
| struct | `whitelist::BuiltinEntry { name: &'static str, reason: &'static str }` | |
| const | `whitelist::BUILTIN: &[BuiltinEntry]` | 26-30 entries, Restart-Manager grounded (A1) |
| fn | `whitelist::builtin_match(pid, basename) -> Option<&'static str>` | PID 0/4 special cases + basename |
| fn | `whitelist::user_match(path, entries) -> bool` | Normalized case-insensitive full path |
| enum | `whitelist::Protection { None, UserConfirm, HardBlocked(&'static str) }` | |
| fn | `whitelist::protection_status(pid, basename, path, &AppSettings) -> Protection` | Built-in tier first (Pitfall 6) |
| enum | `kill::Strategy { WmClose, ConsoleCtrlC, ForceDirect }` | |
| fn | `kill::route_strategy(has_windows, has_console) -> Strategy` | Pure, unit-tested |
| enum | `kill::KillOutcome { Graceful, ForceKilled, Direct, AlreadyExited, AccessDenied, HardBlocked, Failed }` | |
| fn | `kill::kill(snapshot, timeout_secs, on_timeout) -> KillOutcome` | Async, one spawn_blocking scope |
| trait | `ProcessManager` reshaped: `details(&snapshot)`, `terminate(&snapshot, timeout_secs)` | No prior implementors |
| struct | `WindowsProcessManager` | Thin wrapper delegating to free fns |
| settings field | `AppSettings.whitelist: Vec<String>` (serde default) | D-13 |
| settings field | `AppSettings.kill_timeout_secs: u64` (default fn -> 5) | D-02 |
| fn | `default_kill_timeout_secs() -> u64` | |
| messages | `Kill, KillPrepared, KillConfirmed, KillCancelled, KillStart, KillTimeout, KillExecute, KillOutcome` | |
| app fields | `kill_status: Option<KillStatus>`, `kill_in_flight`, `confirm_pid/name/port`, `pending_kill_snapshot`, `last_killed_pid` | |
| types | `KillStatus { text, tone }`, `KillTone { InProgress, Success, Error }` | app.rs |
| fn | `app::format_kill_status(...) -> (String, KillTone)` | 8 UI-SPEC strings + truncation |
| component | `KillConfirmComponent` | 60x7 centered popup |
| CLI flag | `--ctrl-c <pid>` (hidden clap flag) + `helper_send_ctrl_c(pid) -> i32` | Registered in main.rs Task 2 step 10; early-return guard before terminal init (Pitfall 7); exit 0=delivered / 1=no console |
| files | `port-core/tests/kill_integration.rs`, `port-core/tests/process_handle_integration.rs` | First integration tests |
| cargo features | `Win32_Security_WinTrust`, `Win32_System_Console` | Only dependency delta, zero new crates |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI user input (x/y/n keys, selected row) -> core kill API | Untrusted user intent crosses into process termination; the whitelist gate sits here |
| settings.toml (user-writable file) -> whitelist matching -> kill gate | File content directly controls which processes are protected |
| core kill -> Win32 process APIs (OpenProcess/TerminateProcess/PostMessageW) | OS-enforced integrity levels (UIPI/PPL) are the ultimate arbiter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing / Integrity | kill.rs + handle.rs | high | mitigate | ProcessSnapshot creation-time verification (GetProcessTimes FILETIME compare) inside one spawn_blocking scope before any terminate; mismatch -> Failed abort (Pitfall #1, PROC-07); churn test proves no wrong-process kill |
| T-02-05 | Tampering | kill.rs | high | mitigate | Built-in whitelist checked BEFORE OpenProcess (Pitfall #11); every OpenProcess/TerminateProcess/PostMessageW result mapped to a KillOutcome — never swallowed; no false-success reporting (prohibition P5) |
| T-02-02 | Tampering | whitelist.rs | high | mitigate | user_match normalizes (quote/whitespace/trailing-separator strip, case-insensitive full-path compare); unit tests lock the normalization contract (PROC-05 edge truth: no duplicate/no-op semantics); GetLongPathNameW for 8.3 lands with the input validator in plan 02-03 |
| T-02-04 | Spoofing | --ctrl-c helper (port-tui main.rs) | medium | mitigate | Hidden clap flag, numeric PID parse, early-return guard before terminal init (Pitfall 7 — helper mode must exit before raw mode/event loop); helper only signals a console — cannot terminate anything; exit-code contract 0=delivered/1=no console |
| T-02-06 | Spoofing | whitelist.rs BUILTIN | medium | accept | Name/PID matching is heuristic for the non-PPL subset (D-10); PPL on csrss/lsass etc. blocks termination regardless (defense-in-depth); limitation documented in the whitelist overlay copy (plan 02-03) |
| T-02-SC | Tampering | cargo build (dependency supply chain) | high | mitigate | Zero new crates this phase; only two feature flags on the already-locked windows 0.62.2 (RESEARCH Package Legitimacy Audit: Approved, 4.84M wk downloads, Microsoft-maintained, pinned by STATE.md). No install checkpoint needed — no package installs exist |
</threat_model>

<verification>
- `cargo test -p port-core` — unit suites for whitelist/kill/handle/settings (per VALIDATION map TBD-01..04, TBD-06)
- `cargo test -p port-core --test kill_integration --test process_handle_integration` — real-child kill proof (TBD-05, TBD-07)
- `cargo test --workspace` — full suite green before wave merge
- Manual UAT deferred to end-of-phase per human_verify_mode=end-of-phase (VALIDATION map: graceful escalation messages, hard-block UX)
</verification>

<success_criteria>
- A user can select any port and terminate its owning process with x; the escalation pipeline (graceful -> timeout -> force) works against real processes; protected processes cannot be killed; outcomes are visible and truthful
- The kill identity is verified against a ProcessSnapshot creation time — no wrong-process kill possible
- All 8 UI-SPEC status strings render within 80 cols; footer matches the locked contract
</success_criteria>

<output>
Create `.planning/phases/02-process-management-smart-kill/02-01-SUMMARY.md` when done
</output>
