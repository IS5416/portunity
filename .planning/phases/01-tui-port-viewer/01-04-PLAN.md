---
phase: 01-tui-port-viewer
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - E:\MyProducts\Portunity\port-tui\src\components\overview.rs
  - E:\MyProducts\Portunity\port-tui\src\components\history.rs
  - E:\MyProducts\Portunity\port-tui\src\components\traffic.rs
  - E:\MyProducts\Portunity\port-tui\src\components\firewall.rs
  - E:\MyProducts\Portunity\port-tui\src\components\mod.rs
  - E:\MyProducts\Portunity\port-tui\src\app.rs
  - E:\MyProducts\Portunity\port-tui\src\message.rs
  - E:\MyProducts\Portunity\port-tui\src\update.rs
  - E:\MyProducts\Portunity\port-tui\src\main.rs
  - E:\MyProducts\Portunity\Cargo.toml
  - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\SKELETON.md
autonomous: true
requirements:
  - TUI-01
  - TUI-07
  - TUI-08
  - SCAN-04
user_setup: []

must_haves:
  truths:
    - "App opens on the Overview tab (Tab 1) showing summary stats, top 10 ports, and admin status card"
    - "Pressing 1-5 switches between Overview, Ports, History, Traffic, and Firewall tabs"
    - "Tabs 3 (History), 4 (Traffic), and 5 (Firewall) show a centered 'Coming later' placeholder with 'This tab will be available in a future phase. Press 1 or 2 to view active tabs.'"
    - "Tab bar highlights the active tab with Bold + accent_primary bg; inactive tabs are Dim + fg_muted"
    - "When terminal is resized below 80x24, app shows centered 'Terminal too small. Minimum: 80x24. Current: {w}x{h}' message instead of the normal layout"
    - "First render shows the full frame (tab bar + content + status bar + footer) immediately with a scanning spinner — no blank-screen delay"
    - "Release build produces a stripped binary under 10MB: cargo build --release --bin port-tui"
    - "SKELETON.md exists documenting the architectural decisions made in Phase 1"
    - "User can sort by any column in the Ports tab with preserved sort order across manual refreshes"
  artifacts:
    - path: "port-tui/src/components/overview.rs"
      provides: "Overview tab dashboard with summary stats, top ports mini-table, admin card"
    - path: "port-tui/src/components/history.rs"
      provides: "Placeholder Tab 3 — History"
    - path: "port-tui/src/components/traffic.rs"
      provides: "Placeholder Tab 4 — Traffic"
    - path: "port-tui/src/components/firewall.rs"
      provides: "Placeholder Tab 5 — Firewall"
    - path: ".planning/phases/01-tui-port-viewer/SKELETON.md"
      provides: "Walking Skeleton architectural decision record"
  key_links:
    - from: "port-tui/src/main.rs render_app"
      to: "port-tui/src/components/overview.rs"
      via: "Tab 1 dispatch: app.active_tab == 0 → OverviewComponent::render()"
      pattern: "OverviewComponent"
    - from: "port-tui/src/main.rs"
      to: "port-tui/src/app.rs"
      via: "Tab switch: Char('1'-'5') → Message::SwitchTab(index) → app.active_tab = index"
      pattern: "SwitchTab"

---

<objective>
Complete the Phase 1 TUI dashboard: implement the Overview tab (Tab 1) with summary stats, top-10 ports mini-table, and admin status card; implement placeholder tabs (3/4/5) with "Coming later" copywriting; add tab switching via 1-5 keys with highlighted active tab; enforce the 80x24 minimum terminal size gate; optimize the release build profile; and produce the Walking Skeleton architectural decision record (SKELETON.md). End with a human-verified acceptance pass.

Purpose: The Overview tab is the landing page — it fulfills D-14 (default tab) and provides an at-a-glance system state view. Placeholder tabs complete the TUI-01 tab structure. The resize gate ensures TUI-07 compliance. SKELETON.md documents the architecture for subsequent phases.
Output: Complete 5-tab TUI dashboard with overview, port table, and placeholders. Release-optimized binary. SKELETON.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-tui-port-viewer/01-CONTEXT.md
@.planning/phases/01-tui-port-viewer/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
From Plans 01-03 (already created):
- port-core/src/models/connection.rs: Connection { port: Port, process: ProcessInfo, remote_address, remote_port, bytes_sent, bytes_received }
- port-core/src/models/port.rs: Port { number, protocol, state }, Protocol enum, PortState enum
- port-core/src/scanner/mod.rs: scan_all() -> Result<Vec<Connection>>
- port-tui/src/app.rs: App { ports, filtered_ports, scanning, error, is_admin, admin_check_done, search_active, filter_active, sort_column, sort_order, selected_index, should_quit, ... }
- port-tui/src/message.rs: Message enum with all variants from Plans 01-03
- port-tui/src/update.rs: update(app, msg) function
- port-tui/src/main.rs: TEA main loop, render_app(f, app, theme), keyboard dispatch
- port-tui/src/components/ports.rs: PortsComponent implementing Component trait
- port-tui/src/components/mod.rs: Component trait definition
- port-tui/src/theme.rs: Theme struct with One Dark color slots
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab system — Overview tab, placeholder tabs, tab bar interaction, resize gate</name>
  <read_first>
    - E:\MyProducts\Portunity\port-tui\src\app.rs (existing App state — add active_tab field)
    - E:\MyProducts\Portunity\port-tui\src\message.rs (existing Message enum — add SwitchTab)
    - E:\MyProducts\Portunity\port-tui\src\update.rs (existing update — add SwitchTab handler)
    - E:\MyProducts\Portunity\port-tui\src\main.rs (existing main loop — add tab dispatch in render, key handlers for 1-5)
    - E:\MyProducts\Portunity\port-tui\src\components\mod.rs (existing — register new component modules)
    - E:\MyProducts\Portunity\port-tui\src\components\ports.rs (existing — understand render pattern for consistency)
    - E:\MyProducts\Portunity\port-tui\src\theme.rs (existing Theme struct — colors for tab bar rendering)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-UI-SPEC.md (Overview tab layout, placeholder tab copywriting, tab bar visual hierarchy, resize gate, Column Layout collapse priorities, DataTable config)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-CONTEXT.md (D-14 default Overview tab, D-15 first render spinner with full frame)
  </read_first>
  <files>
    E:\MyProducts\Portunity\port-tui\src\app.rs
    E:\MyProducts\Portunity\port-tui\src\message.rs
    E:\MyProducts\Portunity\port-tui\src\update.rs
    E:\MyProducts\Portunity\port-tui\src\main.rs
    E:\MyProducts\Portunity\port-tui\src\components\mod.rs
    E:\MyProducts\Portunity\port-tui\src\components\overview.rs
    E:\MyProducts\Portunity\port-tui\src\components\history.rs
    E:\MyProducts\Portunity\port-tui\src\components\traffic.rs
    E:\MyProducts\Portunity\port-tui\src\components\firewall.rs
  </files>
  <action>
    <step n="1">Extend port-tui/src/app.rs — tab state:
      - Add field: `pub active_tab: usize` — 0-indexed: 0=Overview, 1=Ports, 2=History, 3=Traffic, 4=Firewall
      - Init: active_tab = 0 (D-14: default Overview tab)</step>

    <step n="2">Extend port-tui/src/message.rs — tab switch message:
      - Add: `SwitchTab(usize)` — carries the tab index (0-4)</step>

    <step n="3">Extend port-tui/src/update.rs — tab switch handler:
      - Message::SwitchTab(index) → if index < 5 { app.active_tab = index; } (bounds check). No other state change — tab switch is instant.</step>

    <step n="4">Create port-tui/src/components/overview.rs — Overview tab (Tab 1, D-14, UI-SPEC Overview Tab Layout):
      - `pub struct OverviewComponent;`
      - `impl Component for OverviewComponent { fn render(&self, app: &App, f: &mut Frame, area: Rect, theme: &Theme) }`
      - Layout: split area into 3 vertical sections using Layout::vertical:
        - Top section (~40% height): "Port Summary" on left + "Connection States" on right — horizontal split
        - Middle section (~45% height): "Top Ports" mini-table
        - Bottom section (~15% height): "Admin Status" card
      - **Port Summary panel (top-left):**
        - Block with title "Port Summary" (bold, fg_emphasis)
        - Stats computed from app.ports (the full unfiltered list):
          - Total: app.ports.len()
          - TCP: count where protocol is Tcp or Tcp6
          - UDP: count where protocol is Udp or Udp6
          - IPv4: count from AF_INET scan (Tcp + Udp without v6 suffix) — approximate: count where protocol == Tcp or Udp
          - IPv6: count from AF_INET6 (Tcp6 + Udp6)
        - Render as key-value pairs: "Total:   847" "TCP:     623" "UDP:     224" "IPv4:    789" "IPv6:     58" — numbers in fg_default bold, labels in fg_muted
      - **Connection States panel (top-right):**
        - Block with title "Connection States" (bold, fg_emphasis)
        - Count ports by state:
          - LISTENING: count where port.state == Listen
          - ESTABLISHED: count where port.state == Established
          - TIME_WAIT: count where port.state == TimeWait
          - CLOSE_WAIT: count where port.state == CloseWait
          - SYN_SENT: count where port.state == SynSent
        - Each line: colored symbol (●/○/◉ in state color) + state name + count (right-aligned)
      - **Top Ports mini-table (middle):**
        - Block with title "Top Ports" (bold, fg_emphasis)
        - Show top 10 ports sorted by port number ascending (simplest — not by connection count)
        - Table columns: "Port", "State", "PID", "Process", "Local Addr" — shorter version of main Ports tab table
        - Use Constraint lengths: [7, 6, 6, 15, 15]
        - Rows: first 10 entries from app.ports (or app.filtered_ports if filter/search active)
        - Same color coding as ports tab for state symbols
        - If fewer than 10 ports, show only available rows. If zero ports and not scanning: show "No active ports" in the table area.
      - **Admin Status card (bottom):**
        - Block with title "Admin Status" (bold, fg_emphasis)
        - If app.admin_check_done && app.is_admin: render "Admin: ✓ Full access — all process details available" with ✓ in status_success green
        - If app.admin_check_done && !app.is_admin: render "Admin: ✗ Limited — Press [a] to elevate for full process details" with ✗ in status_warning yellow and [a] in accent_primary underline
        - If !app.admin_check_done: render "Checking admin status..." in fg_muted dim
        - Use Paragraph with alignment::Left within the block
      - All panels use bg_surface background, bordered with fg_muted border style. The card's background is bg_surface to visually separate from bg_base content area.</step>

    <step n="5">Create placeholder tab components — port-tui/src/components/history.rs, traffic.rs, firewall.rs:
      - Each follows identical structure:
        - `pub struct HistoryTabComponent;` (resp. TrafficTabComponent, FirewallTabComponent)
        - `impl Component for XComponent { fn render(...) }`:
          - Render a centered Paragraph within the content area:
            - Text: "Coming later\n\nThis tab will be available in a future\nphase. Press 1 or 2 to view active tabs."
            - Style: fg_muted with dim modifier (Modifier::DIM)
            - Alignment: centered both horizontally and vertically. To center vertically: compute vertical padding as `(area.height.saturating_sub(3)) / 2`, place the paragraph at y = area.y + padding.
            - Block with border style fg_muted, no title
          - Use identical copy for all three tabs per UI-SPEC Placeholder Tabs section.</step>

    <step n="6">Extend port-tui/src/components/mod.rs — register all new components:
      - Add: `pub mod overview; pub mod history; pub mod traffic; pub mod firewall;`
      - Add: `pub use overview::OverviewComponent; pub use history::HistoryTabComponent; pub use traffic::TrafficTabComponent; pub use firewall::FirewallTabComponent;`</step>

    <step n="7">Extend port-tui/src/main.rs — tab bar rendering + tab dispatch + resize gate (TUI-07, D-15):
      - **Tab bar rendering in render_app():**
        - Before content area: render the tab bar in the top row (Constraint::Length(1))
        - Build the tab bar as a Line of Spans:
          - For each tab index 0..4, create a Span:
            - Tab label: " [1] Overview ", " [2] Ports ", " [3] History ", " [4] Traffic ", " [5] Firewall "
            - Active tab (index == app.active_tab): Bold modifier + bg in accent_primary + fg in bg_base (reverse contrast). Use `Style::default().fg(theme.bg_base).bg(theme.accent_primary).add_modifier(Modifier::BOLD)`
            - Inactive tab: Dim modifier + fg_muted + bg_surface. Use `Style::default().fg(theme.fg_muted).add_modifier(Modifier::DIM)`
            - Span separators between tabs: single space in fg_muted
          - Render as Paragraph with bg_surface block background
      - **Content area dispatch in render_app():**
        - After tab bar, the content area renders the active tab component:
        - match app.active_tab:
          - 0 → OverviewComponent.render(app, f, content_area, theme)
          - 1 → PortsComponent.render(app, f, content_area, theme)
          - 2 → HistoryTabComponent.render(app, f, content_area, theme)
          - 3 → TrafficTabComponent.render(app, f, content_area, theme)
          - 4 → FirewallTabComponent.render(app, f, content_area, theme)
          - _ → unreachable (guarded by update bounds check)
      - **Resize gate (TUI-07, UI-SPEC Minimum Size Gate):**
        - At the start of render_app(), before any layout computation:
        - Get terminal size: `let size = f.area();` — this is the current frame's area
        - If `size.width < 80 || size.height < 24`:
          - Render a single centered Paragraph: "Terminal too small\n\nMinimum size: 80 columns x 24 rows.\nCurrent: {w}x{h}\nResize your terminal window to continue."
          - Style: fg_muted, centered, bg_base background
          - Return immediately — do not render the normal layout. The status bar, footer, and tab bar are all suppressed.
        - If `size.width >= 80 && size.height >= 24`: proceed with normal layout. The content area's height is `size.height - 3` (tab bar + status bar + footer = 3 rows), so minimum content height is `24 - 3 = 21` rows. This is well above the Constraint::Min(20) specified in UI-SPEC.
        - This check runs every frame — if the user resizes the terminal mid-session, the gate adapts immediately.
      - **Keyboard dispatch for tab switching (TUI-02 L0 keys):**
        - Add key handlers in the main event loop:
        - Char('1') → Message::SwitchTab(0)
        - Char('2') → Message::SwitchTab(1)
        - Char('3') → Message::SwitchTab(2)
        - Char('4') → Message::SwitchTab(3)
        - Char('5') → Message::SwitchTab(4)
        - Tab → Message::SwitchTab((app.active_tab + 1) % 5) — cycle forward through tabs
        - Shift+Tab detection: KeyCode::BackTab → Message::SwitchTab((app.active_tab + 4) % 5) — cycle backward (wrapping subtract)
        - These keys work even when search/filter is active — tab switching takes priority.
      - **First render guarantees (D-15):**
        - The first frame must show the full layout (tab bar + content + status bar + footer), not a blank screen.
        - On startup: app.scanning = true, so content area shows scanning state (already handled in PortsComponent and OverviewComponent — both check app.scanning first).
        - The tab bar always renders (active_tab defaults to 0).
        - Status bar and footer always render.
        - This is already satisfied by the existing code structure from Plans 01-03. Verify that OverviewComponent also shows a scanning state: if app.scanning, render centered "Scanning ports..." with spinner text in fg_muted, similar to PortsComponent.</step>

    <step n="8">Update OverviewComponent to handle scanning state:
      - If app.scanning: render centered "Scanning ports..." plus a brief "Please wait" — same pattern as PortsComponent. The stats panels show zero counts, which is fine — they update when data arrives.
      - If app.error is Some: render error summary in the admin status card area: "Scan failed: {error} — Press r to retry" in status_error red.</step>

    <step n="9">Run `cargo build --bin port-tui` to verify compilation.</step>
  </action>
  <verify>
    <automated>cargo build --bin port-tui 2>&1 | grep -c 'error' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --bin port-tui` compiles with 0 errors
    - App opens on Overview tab (Tab 1) with Port Summary stats, Connection States count, Top 10 mini-table, and Admin Status card
    - Pressing 1-5 switches tabs; active tab is visually highlighted (Bold + accent_primary background)
    - Inactive tabs show Dim + fg_muted style
    - Tabs 3 (History), 4 (Traffic), 5 (Firewall) show centered "Coming later" + navigation hint
    - Tab key cycles forward through tabs; Shift+Tab cycles backward
    - Terminal < 80x24: centered "Terminal too small" message with current dimensions, normal layout hidden
    - Terminal >= 80x24: normal 4-region layout renders proportionally
    - First frame after launch shows complete layout (tab bar + content + status bar + footer) — no blank screen
    - Overview tab shows scanning spinner when data is loading, stats populate when scan completes
  </acceptance_criteria>
  <done>Tab system complete: 5-tab dashboard with Overview (summary stats + top ports + admin card), Ports (full table), and 3 placeholder tabs. Tab bar highlights active tab. Resize gate enforces 80x24 minimum. First render shows full frame immediately per D-15.</done>
</task>

<task type="auto">
  <name>Task 2: Release build optimization + SKELETON.md architectural record</name>
  <read_first>
    - E:\MyProducts\Portunity\Cargo.toml (root workspace — add release profile)
    - E:\MyProducts\Portunity\.planning\research\ARCHITECTURE.md (architectural decisions made)
    - E:\MyProducts\Portunity\.planning\research\PITFALLS.md (#10 single workspace, #12 WAL mode, #14 allocator)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-CONTEXT.md (all D-01 through D-16 decisions to document)
    - E:\MyProducts\Portunity\.planning\STATE.md (accumulated decisions table)
  </read_first>
  <files>
    E:\MyProducts\Portunity\Cargo.toml
    E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\SKELETON.md
  </files>
  <action>
    <step n="1">Add release profile optimization to root Cargo.toml — append after existing [workspace.dependencies]:
      ```toml
      [profile.release]
      lto = true
      codegen-units = 1
      strip = true
      opt-level = 3
      ```
      These settings produce the smallest, fastest binary per Pitfall #10 recommendation:
      - lto = true: link-time optimization, single compilation unit for cross-crate inlining
      - codegen-units = 1: single codegen unit for maximum optimization (slower compile, smaller binary)
      - strip = true: strip debug symbols from binary
      - opt-level = 3: maximum optimization level
      - Target: < 10MB stripped binary (ROADMAP performance metric)</step>

    <step n="2">Verify release build compiles: run `cargo build --release --bin port-tui`. This may take several minutes due to LTO + single codegen unit. Monitor for compilation errors — if any, fix before proceeding.</step>

    <step n="3">Check binary size:
      - `ls -la target/release/port-tui.exe` (or `dir` equivalent) to get file size
      - Expected: < 10MB. If larger, check for unnecessary crate features or debug symbols. The common culprit is tokio "full" features — but we need those for async runtime. Accept up to 15MB if necessary (the 10MB target is aspirational for a Rust binary with tokio + crossterm + ratatui).</step>

    <step n="4">Create SKELETON.md at E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\SKELETON.md with the following content structure:

      ```markdown
      # Walking Skeleton — Portunity

      **Phase:** 1 — TUI Port Viewer
      **Generated:** 2026-07-26

      ## Capability Proven End-to-End

      A user launches `port-tui.exe` and immediately sees a live table of all active TCP and UDP ports with owning process names and PIDs, color-coded by connection state, sortable by column, filterable by multi-dimension criteria, and fuzzy-searchable across all fields. Admin elevation is auto-detected and offered with a single keypress.

      ## Architectural Decisions

      | Decision | Choice | Rationale |
      |---|---|---|
      | Workspace structure | Single Cargo workspace (root), 3 members: port-core, port-tui, port-gui/src-tauri | Prevents double compilation (Pitfall #10). Single Cargo.lock and target/ directory. Unidirectional dependency: frontends depend on core, never reverse. |
      | Core library design | Async-first trait-based API (PortScanner, ProcessManager, FilterEngine traits) with #[async_trait] | Windows API calls inside spawn_blocking to prevent async runtime stalls (Pitfall #9). Traits provide platform abstraction for future Linux/macOS (Pitfall #3). |
      | TUI architecture | Ratatui Elm Architecture (TEA): centralized Message enum, single update() function, per-tab Component trait | Centralized state management prevents drift between frontends (Anti-Pattern #4). Message-driven rendering is naturally event-driven — no fixed-tick re-render. |
      | Scanner communication | tokio::sync::mpsc::unbounded_channel between scanner spawn tasks and TEA main loop | Scanner runs in spawn_blocking; results arrive via Message::ScanComplete. try_recv on each main loop tick avoids blocking. D-12 decision — costly to change. |
      | Database | SQLite via rusqlite, WAL mode enabled on first connection, PRAGMA busy_timeout=5000 | WAL mode enables concurrent reads during writes (Pitfall #12). rusqlite chosen over sqlx for simplicity and speed (20K queries: 0.069s vs 0.402s). |
      | Configuration | TOML files in %APPDATA%/Portunity/, serde-deserialized, hot-reloadable pattern | TOML chosen over JSON for readability (comments, trailing commas). Hot-reload requires file watcher — deferred to Phase 6. |
      | Port scanning strategy | Dual-stack AF_INET + AF_INET6 enumeration via GetExtendedTcpTable/GetExtendedUdpTable, buffer retry with exponential growth (16KB start, 2x per retry, max 3) | Single AF_INET call misses dual-stack connections (Pitfall #3). Buffer retry prevents silent data loss when table grows between calls (Pitfall #2). Port numbers converted via ntohs (Pitfall #4). |
      | Process name resolution | Batch resolution via sysinfo::System with PID cache (ProcessResolver) | Avoids per-connection OpenProcess calls (D-16). sysinfo provides cross-platform process enumeration without Windows-specific FFI. |
      | Admin elevation | IsUserAnAdmin() detection at startup; ShellExecuteExW with "runas" verb for UAC; old process exits on elevation, new process performs fresh scan | OS-mediated UAC prompt prevents programmatic bypass. No state transfer on restart (D-08) — fresh scan completes in <500ms. |
      | Terminal rendering | crossterm 0.29 backend + ratatui 0.30 widgets, double-buffered, event-driven draw (no fixed-tick re-render) | Event-driven rendering prevents CPU burn at idle (Pitfall #13). Double-buffering + batched writes from crossterm provide flicker-free output (TUI-08). |
      | Theme system | One Dark hardcoded as Theme struct with 12 semantic color slots (fg_default, fg_muted, fg_emphasis, bg_base, bg_surface, bg_overlay, bg_selection, accent_primary, accent_secondary, status_success, status_warning, status_error, status_info) | Semantic slots decouple intent from hex value. Hardcoded in Phase 1; TOML theme files + t-key switching deferred to Phase 6 (TUI-05). |
      | Filter architecture | Free functions in port-core (apply_filters, fuzzy_search) operating on Vec<Connection>, no trait indirection | Filters are platform-independent — no benefit from trait abstraction. Free functions are simpler and just as testable. AND logic across filter dimensions (SRCH-01). |
      | Build toolchain | Rust edition 2024 (1.85+), MSVC toolchain (stable-msvc), tokio 1.50 multi-threaded runtime | Edition 2024 required by workspace resolver = "3". MSVC required for Windows native compilation (windows-rs). Tokio multi-threaded for work-stealing scheduler. |
      | Release optimization | LTO + single codegen unit + strip symbols, opt-level = 3 | Binary size target <10MB stripped. LTO enables cross-crate inlining that significantly reduces tokio's generic monomorphization footprint. |

      ## Stack Touched in Phase 1

      - [x] Project scaffold — Cargo workspace (3 members), all dependencies, module declarations
      - [x] Routing — Tab-based navigation (5 tabs, 1-5 keys, Tab/Shift+Tab cycling)
      - [x] Database — SQLite with WAL mode, settings table, config TOML read/write
      - [x] UI — Full TUI dashboard: overview + port table + placeholder tabs, color-coded, sortable, filterable, searchable
      - [x] Deployment — `cargo build --release --bin port-tui` produces distributable binary; `cargo run --bin port-tui` for dev
      - [x] Windows API integration — GetExtendedTcpTable, GetExtendedUdpTable, IsUserAnAdmin, ShellExecuteExW
      - [x] Process enumeration — sysinfo batch resolution with PID cache
      - [x] Admin elevation — UAC prompt via ShellExecuteExW, non-admin graceful degradation

      ## Out of Scope (Deferred to Later Slices)

      - Process termination (PROC-01 through PROC-07) — Phase 2
      - ETW real-time monitoring (SCAN-05, TRAF-01 through TRAF-03) — Phase 3
      - Port history recording and timeline (HIST-01 through HIST-04) — Phase 3
      - Firewall rule management (FW-01 through FW-06) — Phase 4
      - Data export (EXP-01 through EXP-03) — Phase 4
      - Tauri GUI (GUI-01 through GUI-04) — Phase 5
      - Theme switching with 6 presets (TUI-05) — Phase 6
      - Language toggle Chinese/English (TUI-06, I18N-01 through I18N-03) — Phase 6
      - Auto-labels for known ports (SRCH-04 through SRCH-06) — Phase 6
      - Faceted search with AND/OR logic (SRCH-02) — Phase 2
      - EventBus (CORE-03) — Phase 3
      - Config hot-reload — Phase 6

      ## Subsequent Slice Plan

      Each later phase adds one vertical slice on top of this skeleton without altering its architectural decisions:

      - **Phase 2:** Process management — inspect process details, terminate with smart kill escalation, whitelist-gated protection
      - **Phase 3:** Real-time monitoring — ETW event-driven refresh, traffic sparklines, port history timeline with SQLite append log
      - **Phase 4:** Firewall management — Windows Firewall rule CRUD, right-click quick actions, JSON/CSV export
      - **Phase 5:** Desktop GUI — Tauri v2 + Svelte desktop app with reactive stores, system tray, all Phase 1-4 capabilities
      - **Phase 6:** Polish — auto-labels, custom labels, favorites, 6 theme presets, Chinese/English i18n with Fluent FTL files
      ```
      
      Write this exact content to SKELETON.md. The table uses pipe-separated markdown. All checkboxes use [x] for completed items. The "Out of Scope" list is exhaustive for Phase 1.</step>

    <step n="5">Verify SKELETON.md was written correctly: `wc -l SKELETON.md` should be >80 lines (a substantive document, not a stub).</step>
  </action>
  <verify>
    <automated>cargo build --release --bin port-tui 2>&1 | grep -c 'error' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Root Cargo.toml includes [profile.release] with lto=true, codegen-units=1, strip=true, opt-level=3
    - `cargo build --release --bin port-tui` succeeds with no errors
    - Release binary exists at target/release/port-tui.exe
    - Binary size < 15MB (target: <10MB)
    - SKELETON.md exists at .planning/phases/01-tui-port-viewer/SKELETON.md
    - SKELETON.md documents all architectural decisions with choice + rationale columns
    - SKELETON.md "Stack Touched in Phase 1" section has all 8 checkboxes checked [x]
    - SKELETON.md "Out of Scope" section lists all deferred features with their target phases
    - SKELETON.md "Subsequent Slice Plan" maps Phases 2-6 to user capabilities
  </acceptance_criteria>
  <done>Release build configured and compiles. SKELETON.md documents all architectural decisions from Phase 1 for subsequent phases to build upon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal resize → TUI layout | Terminal size is reported by crossterm from the OS. The resize gate (80x24 check) uses frame dimensions — no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | port-tui/src/main.rs — resize gate | low | accept | The 80x24 gate renders a static message when below minimum — no new allocations per frame. Accepted: the gate is a read-only layout check, not a security boundary. |
| T-04-02 | Information Disclosure | port-tui/src/components/overview.rs — summary stats | low | accept | Overview tab shows aggregate counts (total ports, by protocol, by state) derived from data already visible in the Ports tab. No new information is exposed. |
</threat_model>

<verification>
## End-of-Phase Verification

Run these checks manually and verify each:

1. **Launch:** `cargo run --release --bin port-tui` — app opens on Overview tab with live data
2. **Tabs:** Press 1-5 to verify all 5 tabs render (Overview, Ports, History, Traffic, Firewall)
3. **Port Table:** Switch to Tab 2 — verify sort (s), navigate (j/k), refresh (r)
4. **Search:** Press / — type a port number or process name — verify real-time filtering
5. **Filter:** Press f — fill filter fields — Enter to apply — Esc to clear
6. **Admin:** Verify status bar shows Admin ✓ (admin) or Admin needed — press a to elevate (non-admin)
7. **Resize:** Shrink terminal below 80x24 — verify "Terminal too small" message. Resize back — verify normal layout returns.
8. **Colors:** Verify LISTENING=green ●, ESTABLISHED=blue ●, TIME_WAIT=gray ○, CLOSE_WAIT=yellow ◉
9. **Footer:** Verify context-sensitive footer changes in search mode vs filter mode vs default
10. **Exit:** Press q — verify clean exit to terminal (no artifacts)
</verification>

<success_criteria>
All 5 ROADMAP success criteria for Phase 1 are met:
1. User launches port-tui at 80x24 or larger and sees a table of all active TCP ports (with connection state) and UDP ports, each showing owning process name and PID
2. Connection states are visually distinguishable by color (LISTENING=green, ESTABLISHED=blue, TIME_WAIT=gray, CLOSE_WAIT=yellow) and the user can sort by any column header with preserved sort order across manual refreshes
3. User presses / to open a fuzzy-search bar; the port table filters in real-time as they type across all fields
4. User can apply combined filters (port number range, process name substring, PID, protocol, connection state) and see results update immediately
5. Running without admin rights, the app still displays all ports read-only with a clear indication of which system-owned processes have limited detail; the app auto-detects when admin rights are needed and offers a UAC elevation prompt
</success_criteria>

<output>
Create `E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-04-SUMMARY.md` when done.
</output>
