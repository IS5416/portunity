---
phase: 01-tui-port-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - E:\MyProducts\Portunity\Cargo.toml
  - E:\MyProducts\Portunity\port-core\Cargo.toml
  - E:\MyProducts\Portunity\port-core\src\lib.rs
  - E:\MyProducts\Portunity\port-core\src\scanner\mod.rs
  - E:\MyProducts\Portunity\port-core\src\scanner\tcp.rs
  - E:\MyProducts\Portunity\port-core\src\store\mod.rs
  - E:\MyProducts\Portunity\port-core\src\store\connection.rs
  - E:\MyProducts\Portunity\port-core\src\config\mod.rs
  - E:\MyProducts\Portunity\port-core\src\config\settings.rs
  - E:\MyProducts\Portunity\port-core\src\windows\mod.rs
  - E:\MyProducts\Portunity\port-tui\Cargo.toml
  - E:\MyProducts\Portunity\port-tui\src\main.rs
  - E:\MyProducts\Portunity\port-tui\src\app.rs
  - E:\MyProducts\Portunity\port-tui\src\message.rs
  - E:\MyProducts\Portunity\port-tui\src\update.rs
  - E:\MyProducts\Portunity\port-tui\src\components\mod.rs
  - E:\MyProducts\Portunity\port-tui\src\components\ports.rs
  - E:\MyProducts\Portunity\port-tui\src\theme.rs
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-04
  - CORE-05
  - CORE-06
  - SCAN-01
  - TUI-01
  - TUI-03
  - TUI-07
  - TUI-08
user_setup: []

must_haves:
  truths:
    - "cargo build --workspace compiles without errors"
    - "cargo run --bin port-tui launches a terminal UI showing real TCP port data from the current machine"
    - "Pressing 'q' in the TUI cleanly exits to the terminal (no cursor artifacts, no leftover raw mode)"
    - "Pressing 'r' in the TUI triggers a fresh scan and the table updates with current port data"
    - "SQLite database file is created in the app data directory with WAL journal mode enabled"
    - "Config TOML file exists at %APPDATA%/Portunity/settings.toml with admin_detected field"
  artifacts:
    - path: "port-core/src/scanner/tcp.rs"
      provides: "Windows GetExtendedTcpTable wrapper returning Vec<Connection>"
      exports:
        - "scan_tcp() -> crate::Result<Vec<Connection>>"
    - path: "port-core/src/store/connection.rs"
      provides: "SQLite connection with WAL mode, settings table created"
      exports:
        - "init_db(db_path: &Path) -> crate::Result<Connection>"
    - path: "port-tui/src/app.rs"
      provides: "TEA App state struct"
      exports:
        - "App { ports, scanning, error, last_scan, should_quit }"
    - path: "port-tui/src/components/ports.rs"
      provides: "Port table Component rendering real TCP data"
  key_links:
    - from: "port-tui/src/main.rs"
      to: "port-core/src/scanner/tcp.rs"
      via: "tokio::spawn calls port_core::scanner::scan_tcp(); result sent via UnboundedSender<Message>"
      pattern: "port_core::scanner"
    - from: "port-tui/src/update.rs"
      to: "port-tui/src/components/ports.rs"
      via: "Message::ScanComplete updates App::ports; render loop passes &app.ports to PortsComponent"
      pattern: "ScanComplete"
---

<objective>
Walking Skeleton for Portunity — prove the full architecture works end-to-end: Cargo workspace scaffold, one real Windows TCP scan (AF_INET GetExtendedTcpTable), SQLite init with WAL mode, config TOML defaults, and a working TUI that renders the live port table.

**As a** developer debugging port conflicts, **I want to** launch `port-tui` and see a live table of active TCP ports, **so that** I can verify the tool's core scanning pipeline works before adding filtering, search, and admin features.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tui-port-viewer/01-CONTEXT.md
@.planning/phases/01-tui-port-viewer/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
Executors: Use these existing types directly — no codebase exploration needed.

From port-core/src/models/port.rs:
```rust
pub struct Port { pub number: u16, pub protocol: Protocol, pub state: PortState }
pub enum Protocol { Tcp, Udp, Tcp6, Udp6 }
pub enum PortState { Listen, Established, CloseWait, TimeWait, SynSent, SynReceived, FinWait1, FinWait2, LastAck, Closing, Unknown }
```

From port-core/src/models/process.rs:
```rust
pub struct ProcessInfo { pub pid: u32, pub name: String, pub executable_path: Option<String>, pub command_line: Option<String>, pub start_time: Option<std::time::SystemTime>, pub is_signed: Option<bool>, pub is_system_critical: bool, pub parent_pid: Option<u32> }
```

From port-core/src/models/connection.rs:
```rust
pub struct Connection { pub port: Port, pub process: ProcessInfo, pub remote_address: Option<String>, pub remote_port: Option<u16>, pub bytes_sent: u64, pub bytes_received: u64 }
```

From port-core/src/lib.rs:
```rust
pub type Result<T> = std::result::Result<T, Error>;
pub enum Error { Platform(String), NotFound(String), PermissionDenied(String), Io(#[from] std::io::Error) }
```

From port-core/src/models/filter.rs (filter data model):
```rust
pub struct Filter { pub port_range: Option<(u16, u16)>, pub process_name: Option<String>, pub pid: Option<u32>, pub protocol: Option<Protocol>, pub state: Option<PortState> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete workspace scaffold — all Cargo.toml deps, async trait, module declarations</name>
  <read_first>
    - E:\MyProducts\Portunity\Cargo.toml (workspace root)
    - E:\MyProducts\Portunity\port-core\Cargo.toml
    - E:\MyProducts\Portunity\port-tui\Cargo.toml
    - E:\MyProducts\Portunity\port-core\src\scanner\mod.rs (existing PortScanner trait)
    - E:\MyProducts\Portunity\port-core\src\lib.rs (module declarations)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-CONTEXT.md (D-01 to D-16 for trait shape context)
  </read_first>
  <files>
    E:\MyProducts\Portunity\Cargo.toml
    E:\MyProducts\Portunity\port-core\Cargo.toml
    E:\MyProducts\Portunity\port-tui\Cargo.toml
    E:\MyProducts\Portunity\port-core\src\scanner\mod.rs
    E:\MyProducts\Portunity\port-core\src\lib.rs
  </files>
  <action>
    <step n="1">Root Cargo.toml — add [workspace.dependencies] entries below existing members/resolver lines for: async-trait = "0.1", windows = { version = "0.73", features = ["Win32_NetworkManagement_IpHelper", "Win32_Networking_WinSock", "Win32_System_Threading", "Win32_System_Diagnostics_ToolHelp", "Win32_Security", "Win32_System_ProcessStatus", "Win32_Foundation"] }, sysinfo = "0.39", tokio = { version = "1.50", features = ["full"] }, rusqlite = { version = "0.40", features = ["bundled"] }, serde_json = "1.0", tracing = "0.1", anyhow = "1.0", ratatui = "0.30", crossterm = "0.29", clap = { version = "4.6", features = ["derive"] }, tracing-subscriber = "0.3". Keep existing workspace.package and workspace.dependencies entries (thiserror, chrono, serde) intact — only add new ones.</step>
    <step n="2">port-core/Cargo.toml — add dependencies: async-trait.workspace = true, windows.workspace = true, sysinfo.workspace = true, tokio.workspace = true, rusqlite.workspace = true, serde_json.workspace = true, tracing.workspace = true. Keep existing thiserror, chrono, serde workspace references.</step>
    <step n="3">port-tui/Cargo.toml — add dependencies: port-core = { path = "../port-core" }, ratatui.workspace = true, crossterm.workspace = true, tokio.workspace = true, clap.workspace = true, anyhow.workspace = true, tracing.workspace = true, tracing-subscriber.workspace = true.</step>
    <step n="4">port-core/src/scanner/mod.rs — replace entire file. Make PortScanner trait async using #[async_trait::async_trait] from async-trait crate:
      - trait is `Send + Sync`
      - fn scan(&self) → async fn scan(&self) -> crate::Result&lt;Vec&lt;crate::models::Connection&gt;&gt;;
      - fn scan_process(&self, pid: u32) → async fn scan_process(&self, pid: u32) -> crate::Result&lt;Vec&lt;crate::models::Connection&gt;&gt;;
      - Re-export `pub use tcp::*;` after trait definition.</step>
    <step n="5">port-core/src/lib.rs — add `pub mod store;` and `pub mod config;` after existing module declarations. Ensure export order is: models, scanner, process, firewall, history, traffic, filter, store, config. Add `pub use store::*;` and `pub use config::*;` at bottom.</step>
    <step n="6">Run `cargo check --workspace` to verify all deps resolve and there are no compilation errors. If workspace resolver issues appear, confirm resolver = "3" in root Cargo.toml [workspace] section (already present).</step>
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | grep -c 'error' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --workspace` exits 0 with no error lines
    - port-core/Cargo.toml lists windows 0.73, sysinfo 0.39, tokio 1.50, rusqlite 0.40, async-trait 0.1
    - port-tui/Cargo.toml lists ratatui 0.30, crossterm 0.29, tokio 1.50
    - PortScanner trait methods are async and annotated with #[async_trait]
    - port-core/src/lib.rs declares pub mod store and pub mod config
  </acceptance_criteria>
  <done>All Cargo.toml files have complete dependencies. Workspace compiles (cargo check). PortScanner trait is async-first.</done>
</task>

<task type="auto">
  <name>Task 2: Windows TCP scanner + SQLite WAL init + config TOML defaults</name>
  <reversibility rating="costly">D-12 from CONTEXT.md: the mpsc channel binding between scanner spawn sites and the main loop receiver; changing the channel type or message shape later touches 3+ files</reversibility>
  <read_first>
    - E:\MyProducts\Portunity\port-core\src\scanner\mod.rs (PortScanner trait shape from Task 1)
    - E:\MyProducts\Portunity\port-core\src\models\port.rs (Port, Protocol, PortState enums — map TCP states to variants)
    - E:\MyProducts\Portunity\port-core\src\models\connection.rs (Connection struct fields)
    - E:\MyProducts\Portunity\port-core\src\models\process.rs (ProcessInfo struct fields)
    - E:\MyProducts\Portunity\port-core\src\lib.rs (Error enum variants for error propagation)
    - E:\MyProducts\Portunity\.planning\research\PITFALLS.md (Pitfall #2 buffer pattern, Pitfall #4 byte order, Pitfall #9 blocking, Pitfall #12 WAL mode)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-CONTEXT.md (D-01 buffer retry minimal, D-04 concurrency, D-16 process resolution)
  </read_first>
  <files>
    E:\MyProducts\Portunity\port-core\src\scanner\tcp.rs
    E:\MyProducts\Portunity\port-core\src\store\mod.rs
    E:\MyProducts\Portunity\port-core\src\store\connection.rs
    E:\MyProducts\Portunity\port-core\src\config\mod.rs
    E:\MyProducts\Portunity\port-core\src\config\settings.rs
    E:\MyProducts\Portunity\port-core\src\windows\mod.rs
  </files>
  <action>
    IMPORTANT: This task creates port-core internals consumed by both frontends. All Windows API calls MUST be inside tokio::task::spawn_blocking per Pitfall #9. All public functions return async.

    <step n="1">Create port-core/src/scanner/tcp.rs — implement Windows TCP table enumeration:
      - Use `windows` crate types: `windows::Win32::NetworkManagement::IpHelper::{GetExtendedTcpTable, MIB_TCPTABLE_OWNER_PID, MIB_TCPROW_OWNER_PID, TCP_TABLE_OWNER_PID_ALL}`, plus `windows::Win32::Networking::WinSock::{AF_INET, ntohs}`.
      - Private fn `scan_tcp_table_raw(af: u32) -> crate::Result<Vec<MIB_TCPROW_OWNER_PID>>`:
        - Two-call buffer pattern: first call with null pointer, 0 size → get required size from ERROR_INSUFFICIENT_BUFFER return value
        - Allocate `Vec<u8>` with returned size
        - Second call: cast buffer to `*mut MIB_TCPTABLE_OWNER_PID`, call GetExtendedTcpTable
        - If second call returns ERROR_INSUFFICIENT_BUFFER (table grew): double buffer size once, retry once (D-01 minimal — full exponential retry is Plan 02)
        - If all retries fail: return Error::Platform("TCP scan failed: buffer allocation exhausted")
        - On success: collect dwNumEntries rows, copy each MIB_TCPROW_OWNER_PID into owned Vec
        - Use `ntohs(row.dwLocalPort as u16)` and `ntohs(row.dwRemotePort as u16)` for port byte order conversion (Pitfall #4)
      - Public async fn `scan_tcp() -> crate::Result<Vec<Connection>>`:
        - Wrap everything in `tokio::task::spawn_blocking(move || { ... }).await?` (Pitfall #9)
        - Inside spawn_blocking: call scan_tcp_table_raw(AF_INET)
        - Collect all unique PIDs from the rows
        - Resolve process names: use `sysinfo::System::new_all()` → for each PID, get `system.process(Pid::from(pid as usize))` → extract `.name().to_string_lossy()` → cache by PID (D-16)
        - Map each MIB_TCPROW_OWNER_PID to Connection:
          - Port.number = port number (ntohs already applied)
          - Port.protocol = Protocol::Tcp
          - Port.state = map dwState to PortState enum: MIB_TCP_STATE_LISTEN→Listen, MIB_TCP_STATE_ESTAB→Established, MIB_TCP_STATE_TIME_WAIT→TimeWait, MIB_TCP_STATE_CLOSE_WAIT→CloseWait, MIB_TCP_STATE_SYN_SENT→SynSent, MIB_TCP_STATE_SYN_RCVD→SynReceived, MIB_TCP_STATE_FIN_WAIT1→FinWait1, MIB_TCP_STATE_FIN_WAIT2→FinWait2, MIB_TCP_STATE_LAST_ACK→LastAck, MIB_TCP_STATE_CLOSING→Closing, _→Unknown
          - Process.name = cached process name or "<unknown>" for PID 0 (System Idle Process) or "<access denied>" for PIDs where sysinfo can't resolve
          - Process.pid = dwOwningPid
          - Process.executable_path = None (no per-process OpenProcess — D-16 says avoid per-connection OpenProcess calls)
          - Remote address: format IP from dwRemoteAddr using inet_ntoa-style byte swap (u32::from_be → octets)
          - Set bytes_sent/bytes_received to 0 (traffic monitoring is Phase 3)
        - Return Vec<Connection>
      - Use MIB_TCP_STATE constants from windows crate: `windows::Win32::NetworkManagement::IpHelper::MIB_TCP_STATE_*`
    </step>

    <step n="2">Create port-core/src/windows/mod.rs — implement PortScanner trait for Windows:
      - Replace the placeholder content.
      - Define `pub struct WindowsPortScanner;` (no fields needed — stateless scanner)
      - `#[async_trait::async_trait] impl PortScanner for WindowsPortScanner`:
        - `async fn scan(&self) -> crate::Result<Vec<Connection>>` — delegates to `tcp::scan_tcp()` (UDP is Plan 02)
        - `async fn scan_process(&self, pid: u32) -> crate::Result<Vec<Connection>>` — delegates to `tcp::scan_tcp()` and filters by PID. Return Ok(filtered) or Error::NotFound if PID not in results.
      - Add re-export: `pub use WindowsPortScanner;`</step>

    <step n="3">Create port-core/src/store/mod.rs — module declarations + re-exports:
      - `pub mod connection;`
      - `pub use connection::*;`</step>

    <step n="4">Create port-core/src/store/connection.rs — SQLite init with WAL mode (Pitfall #12):
      - Use `rusqlite::Connection` (not sqlx)
      - Public fn `pub fn init_db(db_path: &std::path::Path) -> crate::Result<rusqlite::Connection>`:
        - Open connection: `Connection::open(db_path)` — map rusqlite::Error to crate::Error::Io
        - Execute `PRAGMA journal_mode=WAL;` — check that returned value is "wal" (Pitfall #12)
        - Execute `PRAGMA busy_timeout=5000;`
        - Execute `CREATE TABLE IF NOT EXISTS settings (key TEXT PRIMARY KEY NOT NULL, value TEXT NOT NULL);`
        - Execute `INSERT OR IGNORE INTO settings (key, value) VALUES ('schema_version', '1');`
        - Return the Connection
      - Public fn `pub fn default_db_path() -> std::path::PathBuf`:
        - Use `std::env::var("APPDATA")` → join "Portunity" → join "portunity.db"
        - Create parent directory (`std::fs::create_dir_all`) if missing</step>

    <step n="5">Create port-core/src/config/mod.rs — module declarations + re-exports:
      - `pub mod settings;`
      - `pub use settings::*;`</step>

    <step n="6">Create port-core/src/config/settings.rs — TOML config:
      - Define `#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)] pub struct AppSettings { pub admin_detected: bool, pub schema_version: i32 }`
      - Public fn `pub fn default_settings() -> AppSettings { AppSettings { admin_detected: false, schema_version: 1 } }`
      - Public fn `pub fn settings_path() -> std::path::PathBuf` — `std::env::var("APPDATA")` → join "Portunity" → join "settings.toml"
      - Public fn `pub fn load_settings() -> crate::Result<AppSettings>`:
        - If settings_path() file does not exist: create parent dir, write default with `std::fs::write(path, toml::to_string_pretty(&default))`, return default
        - If file exists: read, parse TOML, return. On parse error: log warning, return default (not error — app should still run)
      - Public fn `pub fn save_settings(settings: &AppSettings) -> crate::Result<()>`:
        - Ensure parent dir exists
        - Serialize to TOML string via `toml::to_string_pretty`
        - Write to settings_path
      - Add `toml = "0.8"` to port-core/Cargo.toml dependencies (not workspace — config-only dep)</step>

    <step n="7">Add `toml = "0.8"` to port-core/Cargo.toml [dependencies] if not already present.</step>

    <step n="8">Run `cargo test -p port-core` to verify scanner and store compile. If no tests are present yet, run `cargo check -p port-core`.</step>
  </action>
  <verify>
    <automated>cargo build -p port-core 2>&1 | grep -c 'error' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - port-core/src/scanner/tcp.rs exists with public async fn scan_tcp() returning Result<Vec<Connection>>
    - port-core/src/windows/mod.rs has impl PortScanner for WindowsPortScanner
    - GetExtendedTcpTable is called with two-call buffer pattern (first call gets size, second gets data)
    - ntohs() applied to dwLocalPort and dwRemotePort (byte order conversion)
    - All Windows FFI calls are inside tokio::task::spawn_blocking
    - Process names resolved via sysinfo::System, cached by PID
    - port-core/src/store/connection.rs creates SQLite DB with WAL mode, settings table
    - port-core/src/config/settings.rs loads/saves AppSettings TOML
    - `cargo build -p port-core` succeeds with no errors
  </acceptance_criteria>
  <done>port-core has working Windows TCP scanner, SQLite WAL init, and TOML config. `cargo build -p port-core` passes.</done>
</task>

<task type="tracer">
  <name>Task 3 (tracer): TUI event loop — TEA skeleton rendering real TCP port table</name>
  <reversibility rating="costly">D-12 from CONTEXT.md: the tokio::sync::mpsc::unbounded_channel binding between scanner spawn sites and the main loop receiver; changing the channel type or Message::ScanComplete shape later touches both scanner spawn sites and the main loop</reversibility>
  <read_first>
    - E:\MyProducts\Portunity\port-core\src\scanner\mod.rs (PortScanner trait + scan() signature)
    - E:\MyProducts\Portunity\port-core\src\models\connection.rs (Connection struct shape)
    - E:\MyProducts\Portunity\port-core\src\models\port.rs (Port, Protocol, PortState)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-UI-SPEC.md (layout: tab bar + content + status bar + footer, keyboard layers L0-L1, One Dark color slots)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-CONTEXT.md (D-10 event poll with Duration timeout, D-12 mpsc unbounded channel, D-13 single #[tokio::main], D-15 first render spinner)
  </read_first>
  <files>
    E:\MyProducts\Portunity\port-tui\src\main.rs
    E:\MyProducts\Portunity\port-tui\src\app.rs
    E:\MyProducts\Portunity\port-tui\src\message.rs
    E:\MyProducts\Portunity\port-tui\src\update.rs
    E:\MyProducts\Portunity\port-tui\src\components\mod.rs
    E:\MyProducts\Portunity\port-tui\src\components\ports.rs
    E:\MyProducts\Portunity\port-tui\src\theme.rs
  </files>
  <action>
    Wire ONE end-to-end path: Windows TCP scan → mpsc channel → Message::ScanComplete → update() stores in App → render() draws port table. Implement per D-12 (TEA + async bridge), D-10 (crossterm event::poll with Duration timeout), D-13 (single #[tokio::main]), D-15 (first render spinner). No filtering, no UDP, no dual-stack, no admin elevation, no sort — pure tracer.

    <step n="1">Create port-tui/src/message.rs — Message enum per D-12:
      - `pub enum Message { Quit, Tick, Refresh, ScanComplete(Vec<port_core::models::Connection>), ScanError(String) }`
      - Derive Debug.</step>

    <step n="2">Create port-tui/src/app.rs — App state struct:
      - `pub struct App { pub ports: Vec<port_core::models::Connection>, pub scanning: bool, pub error: Option<String>, pub last_scan_time: Option<std::time::Instant>, pub should_quit: bool }`
      - `impl App { pub fn new() -> Self { Self { ports: Vec::new(), scanning: true, error: None, last_scan_time: None, should_quit: false } } }` — scanning starts true so first frame shows spinner (D-15)</step>

    <step n="3">Create port-tui/src/update.rs — TEA update function:
      - `pub fn update(app: &mut App, msg: Message)`: match on message variants
      - Message::Quit → app.should_quit = true
      - Message::Tick → no state change (time-driven re-render handled by main loop)
      - Message::Refresh → app.scanning = true, app.error = None (spawning handled in main loop)
      - Message::ScanComplete(connections) → app.ports = connections, app.scanning = false, app.error = None, app.last_scan_time = Some(Instant::now())
      - Message::ScanError(e) → app.error = Some(e), app.scanning = false (D-03: keep last successful data in ports, only set error)
      - Return None (no chained messages needed for tracer)</step>

    <step n="4">Create port-tui/src/theme.rs — One Dark semantic color slots per UI-SPEC Color section:
      - Define `pub struct Theme { pub fg_default: Color, pub fg_muted: Color, pub fg_emphasis: Color, pub bg_base: Color, pub bg_surface: Color, pub bg_overlay: Color, pub bg_selection: Color, pub accent_primary: Color, pub status_success: Color, pub status_warning: Color, pub status_error: Color, pub status_info: Color }`
      - `pub fn one_dark() -> Theme` with exact values: fg_default = Color::Rgb(171, 178, 191), fg_muted = Color::Rgb(92, 99, 112), fg_emphasis = Color::Rgb(229, 229, 229), bg_base = Color::Rgb(26, 27, 38), bg_surface = Color::Rgb(36, 40, 59), bg_overlay = Color::Rgb(44, 48, 67), bg_selection = Color::Rgb(54, 74, 130), accent_primary = Color::Rgb(97, 175, 239), status_success = Color::Rgb(152, 195, 121), status_warning = Color::Rgb(229, 192, 123), status_error = Color::Rgb(224, 108, 117), status_info = Color::Rgb(86, 182, 194)
      - `pub fn default_theme() -> Theme { one_dark() }` — hardcoded for Phase 1, theme switching is Phase 6</step>

    <step n="5">Create port-tui/src/components/mod.rs — Component trait:
      - `use ratatui::layout::Rect; use ratatui::Frame;`
      - `pub trait Component { fn render(&self, app: &crate::app::App, f: &mut Frame, area: Rect, theme: &crate::theme::Theme); }`
      - `pub mod ports;`
      - `pub use ports::PortsComponent;`</step>

    <step n="6">Create port-tui/src/components/ports.rs — port table component:
      - `pub struct PortsComponent;`
      - `impl Component for PortsComponent`:
        - `fn render(&self, app: &App, f: &mut Frame, area: Rect, theme: &Theme)`:
          - If app.scanning: render centered "Scanning ports..." paragraph in fg_muted, using `Paragraph::new("Scanning ports...").alignment(Alignment::Center).style(Style::default().fg(theme.fg_muted))`, wrapped in a centered block with bg_base. No spinner widget — just text.
          - If app.error is Some: render error message: `Paragraph::new(format!("Scan failed: {}", app.error.as_ref().unwrap())).style(Style::default().fg(theme.status_error))` centered
          - If app.ports is empty and not scanning: render "No active ports" paragraph per UI-SPEC copywriting empty state: `Paragraph::new("No active ports\n\nNo TCP ports are currently in use.").alignment(Alignment::Center).style(Style::default().fg(theme.fg_muted))`
          - If ports exist: build a ratatui::widgets::Table:
            - Header row: `Row::new(vec!["State", "Port", "Proto", "Process", "PID"]).style(Style::default().fg(theme.fg_emphasis).add_modifier(Modifier::BOLD)).bottom_margin(0)`
            - Column widths via Constraint: [Length(6), Length(7), Length(6), Length(18), Length(6)]
            - Data rows: for each Connection in app.ports, map state to Unicode symbol (●), create Row::new(vec![symbol, port_number_string, protocol_string, process_name, pid_string])
            - Row style: alternate bg_base and bg_surface for zebra stripe (use `row_index % 2`). Color symbol based on PortState: LISTENING→status_success (green ●), ESTABLISHED→status_info (blue ●), TIME_WAIT→fg_muted (gray ○), CLOSE_WAIT→status_warning (yellow ◉), all others→fg_muted (gray)
            - Render with `f.render_widget(table, area)`
      - Do NOT use eddacraft-tui DataTable in this tracer task — basic ratatui::widgets::Table only. DataTable upgrade is Plan 02.</step>

    <step n="7">Replace port-tui/src/main.rs — TEA event loop entry point (D-10, D-13):
      - `#[tokio::main]` — multi-threaded runtime (default)
      - Init tracing-subscriber for stderr: `tracing_subscriber::fmt().with_writer(std::io::stderr).init();`
      - crossterm terminal setup:
        - `enable_raw_mode()` — map io::Error to anyhow
        - `execute!(stdout(), EnterAlternateScreen, EnableMouseCapture)` — mouse capture for future UI-SPEC focus management
        - Create `Terminal<CrosstermBackend<Stdout>>` via `Terminal::new(CrosstermBackend::new(stdout()))`
      - Create `let (tx, mut rx) = tokio::sync::mpsc::unbounded_channel::<Message>();` (D-12 unbounded channel)
      - Create `let mut app = App::new();`
      - Create `let theme = default_theme();`
      - Spawn initial scan (D-15 first render):
        - `let tx_clone = tx.clone(); tokio::spawn(async move { let scanner = port_core::windows::WindowsPortScanner; match scanner.scan().await { Ok(conns) => { let _ = tx_clone.send(Message::ScanComplete(conns)); } Err(e) => { let _ = tx_clone.send(Message::ScanError(e.to_string())); } } });`
      - Main event loop:
        - Loop {
          - `terminal.draw(|f| render_app(f, &app, &theme))?;` — full frame redraw each iteration (flicker-free per TUI-08 via crossterm double-buffering)
          - If app.should_quit { break; }
          - `if event::poll(Duration::from_millis(200))?` (D-10): read event via `event::read()?`
            - KeyEvent: match on key.code
              - Char('q') → `Message::Quit`
              - Char('r') → `Message::Refresh`
              - Esc → `Message::Quit` (escape hatch)
            - Send to update(): `update(&mut app, msg);`
          - `while let Ok(msg) = rx.try_recv()`: call `update(&mut app, msg);` (D-12 try_recv on each tick)
          - If Refresh or initial scan completion: handle scan spawning here (the update sets scanning=true; the main loop spawns the task)
            - For refresh: `if app.scanning { ... spawn scan task same as initial scan }` — use a flag to avoid double-spawn
        - }
      - Cleanup (always, via a Drop guard or explicit):
        - `disable_raw_mode()?;`
        - `execute!(stdout(), LeaveAlternateScreen, DisableMouseCapture)?;`

      - `fn render_app(f: &mut Frame, app: &App, theme: &Theme)`:
        - Use `Layout::vertical([Constraint::Length(1), Constraint::Min(0), Constraint::Length(1), Constraint::Length(1)])` to split into 4 regions: tab_bar, content, status_bar, footer (UI-SPEC Layout section)
        - Tab bar region: render `Paragraph::new(format!(" [1] Overview  [2] Ports  [3] History  [4] Traffic  [5] Firewall")).style(Style::default().fg(theme.fg_muted).bg(theme.bg_surface))` — all tabs show, Ports tab not highlighted yet (no active tab state in tracer)
        - Content region: render PortsComponent with `PortsComponent.render(app, f, content_area, theme)`
        - Status bar region: `let status = if app.scanning { format!("Scanning...").to_string() } else if let Some(ref e) = app.error { format!("⚠ {} · Press r to retry", e) } else { format!("Live · {} ports · {}", app.ports.len(), chrono::Local::now().format("%H:%M:%S")) };` render as `Paragraph::new(status).style(Style::default().fg(theme.fg_default).bg(theme.bg_surface))`
        - Footer region: `Paragraph::new("[r]Refresh [q]Quit").style(Style::default().fg(theme.fg_muted).bg(theme.bg_surface)).alignment(Alignment::Center)`</step>

    <step n="8">Run `cargo build --bin port-tui` to verify compilation. Then launch `cargo run --bin port-tui` to verify end-to-end: terminal shows real TCP port data, 'r' refreshes, 'q' exits cleanly. Note: this is an interactive TUI — verify visually that it works, then proceed.</step>
  </action>
  <verify>
    <automated>cargo build --bin port-tui 2>&1 | grep -c 'error' | grep -q '^0$'</automated>
    <human-check>Launch `cargo run --bin port-tui` — verify: (1) terminal shows real active TCP ports with protocol/port/PID/process name columns, (2) pressing 'r' refreshes the table, (3) pressing 'q' exits cleanly to the terminal with no cursor artifacts. If the terminal shows garbled text or cursor disappears on exit, the cleanup (disable_raw_mode + LeaveAlternateScreen) is broken.</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo build --bin port-tui` compiles with 0 errors
    - `cargo run --bin port-tui` launches a terminal UI displaying live TCP port data from Windows IP Helper API
    - First frame shows "Scanning ports..." text briefly, then transitions to port table
    - Port table shows at minimum: state symbol (●/○), port number, protocol (TCP), process name, PID
    - Connection states are visually distinguished by color: LISTENING=green, ESTABLISHED=blue, others=gray
    - Pressing 'r' triggers a visible refresh (brief "Scanning..." status, then updated data)
    - Pressing 'q' cleanly exits to terminal (no leftover raw mode, no cursor artifacts)
    - Status bar shows "Live · N ports · HH:MM:SS" pattern when idle
    - Footer shows "[r]Refresh [q]Quit"
  </acceptance_criteria>
  <done>The walking skeleton is complete. `cargo run --bin port-tui` opens a terminal with a live TCP port table, keyboard refresh works, clean exit. SQLite DB initialized with WAL mode. Config TOML created in app data directory. The full architecture (Windows FFI → port-core scanner → TEA TUI → crossterm render) is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal input → TUI | Keyboard events from the terminal user are trusted (console is local, single-user). Crossterm parses escape sequences. |
| port-core → Windows IP Helper API | FFI boundary — raw C structs cross into safe Rust. windows-rs generated bindings handle allocation safety. |
| TUI → SQLite | Local database file in user's %APPDATA%. No network access. File permissions inherited from user directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | port-core/src/scanner/tcp.rs — GetExtendedTcpTable buffer | low | mitigate | Two-call buffer pattern with size validation (dwNumEntries check). Vec<u8> allocation prevents buffer overflow. Invalid dwNumEntries returns empty result, not panic. |
| T-01-02 | Information Disclosure | port-core/src/store/connection.rs — SQLite DB | low | accept | DB at %APPDATA%/Portunity/portunity.db inherits user directory ACLs. No sensitive data stored in Phase 1 (only schema_version = 1). Future phases store port history — access control review at Phase 3. |
| T-01-03 | Denial of Service | port-tui/src/main.rs — tokio::sync::mpsc::unbounded_channel | low | mitigate | Unbounded channel could theoretically OOM under malicious scanner spam. Mitigation: scanner runs on-demand (not auto-refresh loop in tracer), limiting max in-flight messages to 1. Plan 02 adds bounded channel. |
| T-01-04 | Elevation of Privilege | port-core/src/scanner/tcp.rs — sysinfo process resolution | low | accept | Process enumeration via sysinfo requires no elevation. Phase 1 reads process names only — no process termination, no handle acquisition. |
</threat_model>

<verification>
## End-to-Phase Verification

After all 4 plans complete, verify:
1. `cargo run --bin port-tui` at 80x24: live port table, all 5 tabs, search/filter, admin detection
2. `cargo build --release --bin port-tui` succeeds
3. `cargo test --workspace` passes all tests
4. SKELETON.md documents all architectural decisions
</verification>

<success_criteria>
After this plan: `cargo run --bin port-tui` launches a terminal UI that scans and displays live TCP port data. The walking skeleton architecture (workspace → scanner → TEA → render) is proven. Subsequent plans add UDP, dual-stack, filtering, search, admin elevation, and UI polish on top of this proven foundation.
</success_criteria>

<output>
Create `E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-01-SUMMARY.md` when done.
Also create `E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\SKELETON.md` when all Plan 01 tasks complete.
</output>
