---
phase: 01-tui-port-viewer
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - E:\MyProducts\Portunity\port-core\src\scanner\tcp.rs
  - E:\MyProducts\Portunity\port-core\src\scanner\udp.rs
  - E:\MyProducts\Portunity\port-core\src\scanner\resolver.rs
  - E:\MyProducts\Portunity\port-core\src\scanner\mod.rs
  - E:\MyProducts\Portunity\port-core\src\windows\mod.rs
  - E:\MyProducts\Portunity\port-core\src\lib.rs
  - E:\MyProducts\Portunity\port-tui\Cargo.toml
  - E:\MyProducts\Portunity\port-tui\src\app.rs
  - E:\MyProducts\Portunity\port-tui\src\message.rs
  - E:\MyProducts\Portunity\port-tui\src\update.rs
  - E:\MyProducts\Portunity\port-tui\src\main.rs
  - E:\MyProducts\Portunity\port-tui\src\components\ports.rs
  - E:\MyProducts\Portunity\port-tui\src\components\mod.rs
  - E:\MyProducts\Portunity\port-tui\src\theme.rs
autonomous: true
requirements:
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - TUI-02
  - TUI-04
  - SCAN-01
user_setup: []

must_haves:
  truths:
    - "Port table shows both TCP and UDP entries from the current machine"
    - "TCP IPv4 and IPv6 connections are merged into one unified view (dual-stack enumeration)"
    - "Connection states are visually distinguishable: LISTENING=green ●, ESTABLISHED=blue ●, TIME_WAIT=gray ○, CLOSE_WAIT=yellow ◉, UDP=gray dash"
    - "Pressing 's' toggles sort on the current column: none → ascending (▲) → descending (▼) → none"
    - "Pressing j/k or ↑/↓ navigates the port table selection up/down with reverse-video highlight on the selected row"
    - "Port table scrolls without lag at 500+ entries (VirtualTable or DataTable with viewport-only rendering)"
    - "Port list auto-refreshes every 5 seconds; status bar shows 'Live · N ports · HH:MM:SS'"
    - "On scan failure, previous port data remains visible in the table; status bar shows error in red with 'Press r to retry'"
  artifacts:
    - path: "port-core/src/scanner/udp.rs"
      provides: "GetExtendedUdpTable wrapper"
      exports:
        - "scan_udp() -> crate::Result<Vec<Connection>>"
    - path: "port-core/src/scanner/resolver.rs"
      provides: "Batch PID-to-process-name resolver with cache"
      exports:
        - "ProcessResolver struct, resolve_batch() method"
    - path: "port-tui/src/components/ports.rs"
      provides: "VirtualTable-based port list with sort, color, zebra stripe, row selection"
  key_links:
    - from: "port-tui/src/main.rs"
      to: "port-core/src/scanner"
      via: "auto-refresh timer spawns port_core::scanner::scan_all() combining TCP+UDP via tokio::join!"
      pattern: "scan_all|tokio::join"
    - from: "port-tui/src/update.rs"
      to: "port-tui/src/app.rs"
      via: "Message::Sort(SortColumn) → app.sort_state cycles through SortOrder variants"
      pattern: "Message::Sort"
---

<objective>
Expand the walking skeleton from TCP-only to full dual-stack TCP+UDP scanning with production-quality TUI rendering: color-coded states, column sort with indicators, keyboard row navigation, auto-refresh, and error-resilient status bar. Every decision from CONTEXT.md (D-01 through D-11, D-16) that was deferred in the tracer now lands.

Purpose: The port table is the core deliverable of Phase 1. After this plan, the table is feature-complete for scanning and display — only filtering/search/admin elevation remain.
Output: Complete, production-quality port scanner and port table widget.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-tui-port-viewer/01-CONTEXT.md
@.planning/phases/01-tui-port-viewer/01-UI-SPEC.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
From Plan 01 (already created):
- port-core/src/scanner/tcp.rs: `pub async fn scan_tcp() -> crate::Result<Vec<Connection>>`
- port-core/src/scanner/mod.rs: `#[async_trait] pub trait PortScanner { async fn scan(&self) -> ...; async fn scan_process(&self, pid: u32) -> ...; }`
- port-core/src/windows/mod.rs: `pub struct WindowsPortScanner; impl PortScanner for WindowsPortScanner { ... }`
- port-tui/src/message.rs: `pub enum Message { Quit, Tick, Refresh, ScanComplete(...), ScanError(...) }`
- port-tui/src/app.rs: `pub struct App { pub ports: Vec<Connection>, pub scanning: bool, pub error: Option<String>, ... }`
- port-tui/src/update.rs: `pub fn update(app: &mut App, msg: Message)`
- port-tui/src/components/ports.rs: `pub struct PortsComponent; impl Component for PortsComponent { fn render(...) }`
- port-tui/src/theme.rs: `pub struct Theme { ... }; pub fn default_theme() -> Theme`
- port-tui/src/main.rs: TEA main loop with crossterm, mpsc channel, render_app function
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scanner completeness — dual-stack, UDP, retry, concurrency, batch process resolution</name>
  <read_first>
    - E:\MyProducts\Portunity\port-core\src\scanner\tcp.rs (existing TCP scanner from Plan 01 — extend with AF_INET6 and retry)
    - E:\MyProducts\Portunity\port-core\src\scanner\mod.rs (existing async PortScanner trait)
    - E:\MyProducts\Portunity\port-core\src\models\port.rs (Protocol enum: Tcp, Udp, Tcp6, Udp6)
    - E:\MyProducts\Portunity\port-core\src\models\connection.rs (Connection struct)
    - E:\MyProducts\Portunity\port-core\src\windows\mod.rs (existing WindowsPortScanner impl — extend with UDP)
    - E:\MyProducts\Portunity\.planning\research\PITFALLS.md (Pitfall #1 PID reuse context, Pitfall #2 buffer retry, Pitfall #3 dual-stack, Pitfall #4 byte order)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-CONTEXT.md (D-01 exponential retry, D-02 dual-stack merge, D-04 tokio::join!, D-16 batch process resolution)
  </read_first>
  <files>
    E:\MyProducts\Portunity\port-core\src\scanner\tcp.rs
    E:\MyProducts\Portunity\port-core\src\scanner\udp.rs
    E:\MyProducts\Portunity\port-core\src\scanner\resolver.rs
    E:\MyProducts\Portunity\port-core\src\scanner\mod.rs
    E:\MyProducts\Portunity\port-core\src\windows\mod.rs
    E:\MyProducts\Portunity\port-core\src\lib.rs
  </files>
  <action>
    <step n="1">Extend port-core/src/scanner/tcp.rs — add dual-stack AF_INET6 (D-02) and exponential buffer retry (D-01):
      - The existing scan_tcp_table_raw takes `af: u32` as parameter — already generic enough. Keep it.
      - **Buffer retry (D-01):** Replace the simple "retry once" logic in scan_tcp_table_raw with exponential growth. Start buffer at 16384 bytes (16KB). After each ERROR_INSUFFICIENT_BUFFER, double the allocation. Max 3 retries. Track the required size returned by the OS on each attempt. If all retries exhausted: return Error::Platform("TCP scan failed after 3 buffer retries").
      - **AF_INET6 path:** In the public async fn scan_tcp(), after getting AF_INET results:
        - Try calling scan_tcp_table_raw with `windows::Win32::Networking::WinSock::AF_INET6` as af parameter
        - AF_INET6 requires using `MIB_TCP6TABLE_OWNER_PID` and `MIB_TCP6ROW_OWNER_PID` types. The buffer pattern is identical but the struct cast differs.
        - Create a sister function `scan_tcp6_table_raw() -> Result<Vec<MIB_TCP6ROW_OWNER_PID>>` that mirrors scan_tcp_table_raw but uses the AF_INET6 types
        - The AF_INET6 table uses `TCP_TABLE_OWNER_PID_ALL` (value 5) — same table class constant (Pitfall #3)
        - Process MIB_TCP6ROW_OWNER_PID rows identically: ntohs on ports, map dwState to PortState
        - Build Connection with Protocol::Tcp6 for IPv6-sourced entries
        - Merge AF_INET and AF_INET6 results into single Vec<Connection>
        - **Critical D-02:** An IPv4-mapped IPv6 address `::ffff:a.b.c.d` represents the same endpoint. After merging: scan for duplicate (Protocol, local_port) where one entry is Tcp6 with IPv4-mapped address and another is Tcp. Keep the AF_INET entry (canonical), drop the IPv4-mapped Tcp6 duplicate. Use a HashSet<(u16, Protocol)> of (local_port, normalized_protocol) to track.
      - Return merged Vec<Connection> with all entries tagged correctly (Tcp or Tcp6 protocol variant)</step>

    <step n="2">Create port-core/src/scanner/udp.rs — GetExtendedUdpTable wrapper (SCAN-02):
      - Mirror the TCP scanner pattern. Use `GetExtendedUdpTable` with `UDP_TABLE_OWNER_PID` table class.
      - Public async fn `scan_udp() -> crate::Result<Vec<Connection>>`:
        - Inside tokio::task::spawn_blocking:
        - Call GetExtendedUdpTable with AF_INET → `MIB_UDPTABLE_OWNER_PID` / `MIB_UDPROW_OWNER_PID`
        - Call GetExtendedUdpTable with AF_INET6 → `MIB_UDP6TABLE_OWNER_PID` / `MIB_UDP6ROW_OWNER_PID`
        - Same buffer retry pattern (D-01), same byte order conversion (ntohs)
        - UDP has no connection state — all entries get PortState::Unknown (Pitfall #3: "UDP has no connection states")
        - Map to Connection with Protocol::Udp or Protocol::Udp6
        - No remote address/port for UDP (listening sockets only show local endpoint)
        - Apply same IPv4-mapped IPv6 deduplication as TCP
        - Return Vec<Connection>
      - Like TCP, process resolution happens after merge in a shared step — not inside this function</step>

    <step n="3">Create port-core/src/scanner/resolver.rs — batch PID-to-process-name resolution (D-16):
      - `pub struct ProcessResolver { cache: HashMap<u32, String> }`
      - `impl ProcessResolver { pub fn new() -> Self; pub fn resolve_batch(&mut self, pids: &[u32]) -> crate::Result<()>; pub fn get(&self, pid: u32) -> Option<&str>; }`
      - resolve_batch:
        - Collect PIDs not in cache
        - Initialize sysinfo::System once: `let mut system = System::new_all();`
        - For each uncached PID: call `system.refresh_process(Pid::from(pid as usize))` — avoid full refresh_all per call
        - For each PID, get `system.process(Pid::from(pid as usize))`, extract name via `.name().to_string_lossy().to_string()`
        - PID 0 → cache as "System Idle Process"
        - PID 4 → cache as "System" (Windows kernel pseudo-process)
        - Failed resolution → cache as "<unknown>" (not an error — some processes are protected)
        - Update cache with resolved names
      - get: simple HashMap lookup returning Option<&str></step>

    <step n="4">Add public combined scan function in port-core/src/scanner/mod.rs:
      - `pub async fn scan_all() -> crate::Result<Vec<Connection>>`:
        - Per D-04: use `tokio::join!(tcp::scan_tcp(), udp::scan_udp())` for concurrent TCP+UDP scanning
        - Merge results: `let mut all = tcp_results?; all.extend(udp_results?);`
        - Collect all unique PIDs from all connections
        - Call `ProcessResolver::new().resolve_batch(&pids)` to batch-resolve all process names
        - For each connection: set `process.name = resolver.get(conn.process.pid).unwrap_or("<unknown>").to_string()`
        - Return merged Vec
      - Re-export: `pub use tcp::scan_tcp;`, `pub use udp::scan_udp;`, `pub use resolver::ProcessResolver;`</step>

    <step n="5">Update port-core/src/windows/mod.rs — wire UDP into WindowsPortScanner:
      - `async fn scan(&self)` → now calls `scanner::scan_all()` instead of just `tcp::scan_tcp()`
      - `async fn scan_process(&self, pid: u32)` → calls `scanner::scan_all()` then filters by PID</step>

    <step n="6">Update port-core/src/lib.rs — add new scanner sub-modules:
      - `pub mod process;` is already declared. Scanner sub-modules (tcp.rs, udp.rs, resolver.rs) are re-exported from scanner/mod.rs — ensure `pub use scanner::*;` is present for scan_all to be accessible as `port_core::scan_all`.</step>

    <step n="7">Run `cargo build -p port-core` and `cargo test -p port-core` (if tests exist) to verify compilation.</step>
  </action>
  <verify>
    <automated>cargo build -p port-core 2>&1 | grep -c 'error' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - scanner/tcp.rs: scan_tcp_table_raw uses exponential buffer growth (16KB start, double, max 3 retries per D-01)
    - scanner/tcp.rs: scan_tcp() calls both AF_INET and AF_INET6, merges results, deduplicates IPv4-mapped IPv6 (D-02)
    - scanner/udp.rs: scan_udp() calls GetExtendedUdpTable for both AF_INET and AF_INET6, returns Vec<Connection> with Protocol::Udp/Udp6
    - scanner/resolver.rs: ProcessResolver caches PID→name lookups, batch-resolves via sysinfo
    - scanner/mod.rs: scan_all() uses tokio::join! for concurrent TCP+UDP (D-04), batch-resolves PIDs (D-16)
    - windows/mod.rs: WindowsPortScanner::scan() delegates to scan_all()
    - cargo build -p port-core succeeds with 0 errors
  </acceptance_criteria>
  <done>Scanner is production-complete: dual-stack TCP+UDP enumeration with exponential buffer retry, concurrent scanning via tokio::join!, batch process name resolution with PID cache. All per D-01, D-02, D-04, D-16.</done>
</task>

<task type="auto">
  <name>Task 2: TUI enhancements — DataTable, sort, color map, keyboard nav, auto-refresh</name>
  <read_first>
    - E:\MyProducts\Portunity\port-tui\src\main.rs (existing TEA main loop from Plan 01)
    - E:\MyProducts\Portunity\port-tui\src\app.rs (existing App state)
    - E:\MyProducts\Portunity\port-tui\src\message.rs (existing Message enum)
    - E:\MyProducts\Portunity\port-tui\src\update.rs (existing update function)
    - E:\MyProducts\Portunity\port-tui\src\components\ports.rs (existing PortsComponent — rewrite)
    - E:\MyProducts\Portunity\port-tui\src\theme.rs (existing Theme — may need accent_secondary addition)
    - E:\MyProducts\Portunity\port-core\src\models\port.rs (PortState variants for color mapping)
    - E:\MyProducts\Portunity\port-core\src\models\connection.rs (Connection shape)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-UI-SPEC.md (DataTable config, keyboard layers, sort interaction, status bar patterns, footer patterns, color map)
    - E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-CONTEXT.md (D-03 error resilience, D-10 event poll timeout, D-11 auto-refresh 5s)
  </read_first>
  <files>
    E:\MyProducts\Portunity\port-tui\Cargo.toml
    E:\MyProducts\Portunity\port-tui\src\app.rs
    E:\MyProducts\Portunity\port-tui\src\message.rs
    E:\MyProducts\Portunity\port-tui\src\update.rs
    E:\MyProducts\Portunity\port-tui\src\main.rs
    E:\MyProducts\Portunity\port-tui\src\components\ports.rs
    E:\MyProducts\Portunity\port-tui\src\components\mod.rs
    E:\MyProducts\Portunity\port-tui\src\theme.rs
  </files>
  <action>
    This task upgrades the basic Table from the tracer to a production-quality virtual-scrolling DataTable with sort, full color mapping, keyboard row navigation, auto-refresh, and the complete status bar/footer patterns from UI-SPEC. All per SCAN-03 (color), SCAN-04 (sort), TUI-02 (keyboard), TUI-04 (VirtualTable), D-03 (error resilience), D-11 (auto-refresh).

    <step n="1">Add eddacraft-tui to port-tui/Cargo.toml:
      - Under [dependencies], add `eddacraft-tui = "0.4"` (after the existing deps).
      - Run `cargo check -p port-tui` to confirm dependency resolution.</step>

    <step n="2">Extend port-tui/src/theme.rs — add accent_secondary color slot for future Phase 2:
      - Add `pub accent_secondary: Color` field to Theme struct
      - Set accent_secondary = Color::Rgb(198, 120, 221) in one_dark() function
      - This slot is not used in Phase 1 but prevents a future struct-breaking change. No theme switching logic added (Phase 6).</step>

    <step n="3">Extend port-tui/src/message.rs — add sort and navigation variants:
      - Add variants before closing brace: `Sort(SortColumn)`, `MoveUp`, `MoveDown`, `ScrollTop`, `ScrollBottom`
      - Define `pub enum SortColumn { Port, Protocol, State, ProcessName, Pid }` with `impl SortColumn { pub fn next(&self) -> Self { ... } }` — cycles through variants
      - Define `pub enum SortOrder { None, Ascending, Descending }` with `impl SortOrder { pub fn cycle(&self) -> Self { ... } }` — cycles None→Ascending→Descending→None</step>

    <step n="4">Extend port-tui/src/app.rs — add sort state, selection, and auto-refresh tracking:
      - Add fields to App struct: `pub sort_column: SortColumn`, `pub sort_order: SortOrder`, `pub selected_index: usize`, `pub last_auto_refresh: Option<std::time::Instant>`
      - Init in App::new(): sort_column = SortColumn::Port, sort_order = SortOrder::None, selected_index = 0, last_auto_refresh = None</step>

    <step n="5">Extend port-tui/src/update.rs — sort logic and navigation:
      - Add match arms for new Message variants:
      - Message::Sort(col) → if app.sort_column == col { app.sort_order = app.sort_order.cycle(); } else { app.sort_column = col; app.sort_order = SortOrder::Ascending; } — then sort app.ports in-place per sort column and order:
        - SortOrder::None → no sorting (keep insertion order from scanner)
        - SortOrder::Ascending → sort by column ascending (Port: by number; ProcessName: by lowercase string; Pid: by u32; State: by PortState discriminants; Protocol: by enum discriminants)
        - SortOrder::Descending → reverse of ascending
        - Use `app.ports.sort_by(|a, b| { ... })` — stable sort not required since scanner returns definitive order
      - Message::MoveUp → if app.selected_index > 0 { app.selected_index -= 1; }
      - Message::MoveDown → if app.selected_index + 1 < app.ports.len() { app.selected_index += 1; }
      - Message::ScrollTop → app.selected_index = 0;
      - Message::ScrollBottom → app.selected_index = app.ports.len().saturating_sub(1);
      - On Message::ScanComplete: reset selected_index to 0 if ports changed significantly; preserve sort by re-applying current sort_column + sort_order</step>

    <step n="6">Rewrite port-tui/src/components/ports.rs — production-quality virtual port table (TUI-04, SCAN-03):
      - Replace basic Table with eddacraft_tui::DataTable where available, or implement custom virtual scrolling if DataTable API doesn't suit. Approach: use ratatui::widgets::Table with manual viewport clipping.
      - **Virtual scrolling (TUI-04):** Compute visible row range from terminal height. If there are more rows than fit, only render the visible slice. Track scroll_offset in the component (not in App — component-local state). When selected_index moves outside viewport, adjust scroll_offset.
      - **Color map (SCAN-03) — full mapping per UI-SPEC:**
        - LISTENING → theme.status_success, symbol "●"
        - ESTABLISHED → theme.status_info, symbol "●"
        - TIME_WAIT → theme.fg_muted, symbol "○"
        - CLOSE_WAIT → theme.status_warning, symbol "◉"
        - SYN_SENT → theme.status_error, symbol "●"
        - SYN_RECEIVED → theme.fg_muted, symbol "○"
        - FIN_WAIT1 / FIN_WAIT2 → theme.fg_muted, symbol "○"
        - LAST_ACK / CLOSING → theme.fg_muted, symbol "○"
        - UNKNOWN (UDP and other) → theme.fg_muted, symbol "—"
        - All symbols use the color of their state — no symbol is ever in fg_default.
      - **Sort indicator (SCAN-04):** Column header text for the active sorted column includes a sort arrow: "Port ▲" or "Port ▼". Other columns have no arrow. Append indicator to the header Cell's content string.
      - **Row selection (TUI-02):** If selected_index matches the row index AND the row is visible, render that entire row with reverse video (Style::default().add_modifier(Modifier::REVERSED)). The reverse video overrides the per-cell color, which is expected terminal behavior.
      - **Zebra stripe:** Even rows use bg_base, odd rows use bg_surface.
      - **Column widths:** State=6, Port=7, Proto=6, Process=18, PID=6 — use Constraint::Length for each.
      - **Truncation:** Use `.to_string()` on each cell and let ratatui truncate at column boundary. Process names >18 chars show "…" via ratatui's built-in truncation. No multi-line rows.
      - **Scrollbar:** Render on the right edge of the table area: `│` for track, `█` for thumb, `░` for scrollable area. Use ratatui-widgets scrollbar if available (from tui-widgets 0.7.10), otherwise manual.
      - For the DataTable/Table widget: pass a Row iterator constructed from the visible slice only. Use Row::new with the same number of cells as the header.</step>

    <step n="7">Extend port-tui/src/main.rs — auto-refresh, keyboard navigation, and expanded footer (D-11, TUI-02):
      - **Auto-refresh (D-11):** In the main event loop, after the terminal.draw() call:
        - Track last_auto_refresh time: `let auto_refresh_interval = Duration::from_secs(5);`
        - If `app.last_scan_time` is Some AND elapsed since last_scan_time > 5s: trigger a background scan (same pattern as manual refresh). Update last_scan_time after the scan completes (in ScanComplete handler), not when spawned.
        - Auto-refresh must NOT interrupt the user mid-interaction: only trigger when not scanning and no error is active. Use `if !app.scanning && app.error.is_none() && elapsed > auto_refresh_interval { ... }`
      - **Keyboard navigation (TUI-02):** Add key event handlers in the main loop:
        - Char('j') or KeyCode::Down → tx.send(Message::MoveDown)
        - Char('k') or KeyCode::Up → tx.send(Message::MoveUp)
        - Char('g') → tx.send(Message::ScrollTop)
        - Char('G') → tx.send(Message::ScrollBottom) (uppercase G, shifted)
        - Char('s') → tx.send(Message::Sort(app.sort_column)) — lower case 's' for sort
        - Keep existing 'r' → Refresh, 'q' → Quit, Esc → Quit
      - **Footer update — change from "[r]Refresh [q]Quit" to full footer per UI-SPEC:**
        - Default: `"[↑↓jk]Navigate [s]Sort [r]Refresh [q]Quit [?]Help"`
        - Style: fg_muted, bg_surface, centered. Hotkey characters ('s', 'r', 'q', '?') should have accent_primary underline — use Span::styled for each segment: `Line::from(vec![Span::styled("[↑↓jk]", Style::default().fg(theme.fg_muted)), Span::raw(" "), Span::styled("Navigate", Style::default().fg(theme.fg_muted)), ...])`
        - Tab hints `[1-5]Tabs` are NOT included yet — overview tab + placeholder tabs arrive in Plan 04. Search/filter hints NOT included yet — arrive in Plan 03.
      - **Status bar update:** Keep the pattern from Plan 01 but expand error case:
        - Error state (D-03): `"⚠ {error_message} · Press r to retry"` in theme.status_error color, on bg_surface background
        - Scanning: `"Scanning... · {N_so_far} found so far · {HH:MM:SS}"` — show partial count from current ports
        - Idle: `"Live · {N} ports · HH:MM:SS"` — keep existing pattern
        - Admin indicator NOT added yet (Plan 03)</step>

    <step n="8">Run `cargo build --bin port-tui` to verify compilation. Then `cargo run --bin port-tui` for visual verification.</step>
  </action>
  <verify>
    <automated>cargo build --bin port-tui 2>&1 | grep -c 'error' | grep -q '^0$'</automated>
    <human-check>Launch `cargo run --bin port-tui` and verify: (1) table shows both TCP and UDP entries, (2) colors match UI-SPEC: LISTENING=green ●, ESTABLISHED=blue ●, TIME_WAIT=gray ○, CLOSE_WAIT=yellow ◉, UDP=gray dash, (3) pressing 's' cycles sort on the port column (▲ → ▼ → no indicator), (4) j/k or arrow keys move selection highlight up/down, (5) footer shows "[↑↓jk]Navigate [s]Sort [r]Refresh [q]Quit [?]Help", (6) wait 5 seconds — table auto-refreshes without user action, (7) status bar shows "Live · N ports · HH:MM:SS".</human-check>
  </verify>
  <acceptance_criteria>
    - eddacraft-tui 0.4 is listed in port-tui/Cargo.toml dependencies
    - Port table renders TCP and UDP connections merged in one unified view
    - All TCP connection states are color-mapped exactly per UI-SPEC Color section
    - Sort cycles through column (none→ascending▲→descending▼→none) on 's' key
    - j/k and ↑/↓ navigate row selection; selected row is reverse-video highlighted
    - Virtual scrolling: at 500+ ports, only visible rows are rendered (no scroll lag)
    - Auto-refresh triggers every 5 seconds without interrupting user interaction (D-11)
    - On scan failure: existing port data remains visible in table, status bar shows red error with "Press r to retry" (D-03)
    - Footer shows navigation, sort, refresh, quit, help hints per UI-SPEC footer contract
    - `cargo build --bin port-tui` compiles with 0 errors
  </acceptance_criteria>
  <done>The port table is production-quality: dual-stack TCP+UDP with full color mapping, sortable columns, keyboard row navigation, virtual scrolling for large lists, and 5-second auto-refresh. The TUI now matches the UI-SPEC scanner display requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal input → TUI | Keyboard events processed by crossterm. Navigation keys (j/k/g/G/s) are safe — they only mutate App state, not execute system commands. |
| port-core → GetExtendedUdpTable | Same FFI boundary as TCP scanner from Plan 01. UDP table results are parsed identically. |
| sysinfo → process names | Process name strings from sysinfo are sanitized (to_string_lossy). No raw OS strings escape the scanner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | port-core/src/scanner/tcp.rs — buffer retry loop | low | mitigate | Exponential growth capped at 3 retries prevents infinite allocation. The max buffer size is bounded (16KB × 2^3 = 128KB). |
| T-02-02 | Information Disclosure | port-core/src/scanner/resolver.rs — process name cache | low | accept | Cache holds process names from sysinfo. These are already visible to the user via Task Manager. No new information exposed. |
| T-02-03 | Denial of Service | port-tui/src/main.rs — auto-refresh loop | low | mitigate | Auto-refresh is guarded: only triggers when not already scanning AND no error is active. Maximum one concurrent scan at a time. 5-second interval is human-scale. |
</threat_model>

<verification>
## Plan-Level Checks

1. `cargo build -p port-core` — scanner compiles with dual-stack + UDP + retry
2. `cargo build --bin port-tui` — TUI compiles with DataTable, sort, keyboard nav, auto-refresh
3. `cargo run --bin port-tui` — visual: all states color-coded, sortable, scrollable, auto-refresh works
</verification>

<success_criteria>
After this plan: the port table is feature-complete for display. All TCP states are color-coded per SCAN-03. Both TCP and UDP ports are visible (SCAN-01, SCAN-02). Columns are sortable (SCAN-04). Keyboard row navigation works (TUI-02). Virtual scrolling handles 500+ ports (TUI-04). Auto-refresh runs every 5 seconds. Error resilience preserves last data on scan failure.
</success_criteria>

<output>
Create `E:\MyProducts\Portunity\.planning\phases\01-tui-port-viewer\01-02-SUMMARY.md` when done.
</output>
